# FALLO_DE_AMBIENTE — 09.5.11C Revalidar Goals 07–09

El Goal 09.5.11C no cierra. Misiones y primera señal revalidan; voz no queda
probada por el comando dueño. No se avanza a 09.5.12 hasta que la prueba de
readiness salga verde. El mismo prompt
[`documentacion/sprints/09.5.11C_REVALIDAR_07_09.md`](../../../documentacion/sprints/09.5.11C_REVALIDAR_07_09.md)
se reanuda cuando el PC cumpla esa prueba.

`next_human_prompt` del ledger/synthesis permanece en
`documentacion/sprints/09.5.12_INTEGRAR_Y_REPLANIFICAR.md` (no se remite al
owner). Eso nombra el siguiente pegado **después** de un cierre honesto; no
declara el cierre.

## Fuentes afectadas

1. Intérprete `py -3.12`
   (`C:\Users\emman\AppData\Local\Programs\Python\Python312\python.exe`)
   — **ausentes** `silero_vad` y `sounddevice`. Presentes: `livekit.wakeword`,
   `sherpa_onnx`, `onnxruntime`, `torch`. El bundle Parakeet, el ONNX wake y el
   modelo neural TTS sí están en `BAXYRuntime`.
2. `NeuralSpeechOutput.start()` en ese intérprete →
   `tts_unavailable:ModuleNotFoundError` (`sounddevice`).
3. `VoiceEngine.load()` → `ModuleNotFoundError: silero_vad`.

No es el fallo: el Python del manifiesto
`%LOCALAPPDATA%\BAXYRuntime\mind-runtime-v1.json` importa `silero_vad` y
`sounddevice`, y `NeuralSpeechOutput.start()` devuelve True. Eso prueba el
sidecar de producto, no el comando dueño
`py -3.12 -m pytest tests/test_goal095_09511c_revalidate.py tests/test_goal09_voice_engines.py -q`.

El close de `c28cd62` afirmó 7/7 voice tests con
`runtime-python pytest`. Esa corrida no sustituye `py -3.12`.

## Evidencia

- `py -3.12 -m pytest tests/test_goal095_09511c_revalidate.py tests/test_goal09_voice_engines.py -q`
  sobre `c28cd62`: 11 passed, 4 failed (TTS `start()` False;
  tres STT/wake-engine por `silero_vad`).
- El Python de `%LOCALAPPDATA%\BAXYRuntime\python\mind-runtime-v1\Scripts\python.exe`
  corre `tests/test_goal09_voice_engines.py -q`: 7 passed in 42.63s. Eso no
  sustituye `py -3.12`.
- `from livekit.wakeword import WakeWordModel`, `import sherpa_onnx`,
  `import onnxruntime` en `py -3.12`: OK. El diagnóstico abortado
  «livekit/sherpa_onnx/backend TTS (onnxruntime) ausentes» no es el de esta
  máquina.
- Cuatro extractos pre-campaña: no restaurados. `_evidence_profile.txt` blob
  `53e36625c8fef324ae21d7ce24cbfdc26f00c405` y
  `_goal095_profile_evidence.py` blob
  `f1ff21f16e572cf6a3be7ce47f764691b017e4ca` en `5405efa`, retirados por
  `bdb8919`. `_schema_agent_blobs/` nunca versionado; identidad
  `artifacts/goal095/sources/baxy_schema_agent.keep.sha256.jsonl` blob
  `0205119bcc86e1ecdcca54e9c2133551cf085ae9`. Fragments Carter v2 nunca
  versionado; fuente
  `code_tests-002-carter-carter_legacy_Carter_v2.inspect.json` blob
  `3d02a3549f9e4e232e3d7211a8e78c983b863670`. Hashes not invented.

## Preparación manual exacta

En el mismo `py -3.12` que corre pytest (no en el venv de `BAXYRuntime`):

1. Instalar `silero_vad` y `sounddevice` en ese intérprete.
2. No sustituir el comando dueño por el Python del runtime.
3. No marcar skip ni relajar aserciones.

## Prueba de readiness (antes de relanzar)

Desde la raíz del repo. Tiene que salir 15 passed, 0 failed, 0 skipped.

```powershell
py -3.12 -c "import silero_vad, sounddevice; print('READY_IMPORTS=1')"
py -3.12 -m pytest tests/test_goal095_09511c_revalidate.py tests/test_goal09_voice_engines.py -q
```

Si `READY_IMPORTS=1` y pytest es 15 passed / 0 failed / 0 skipped, relanzar el
mismo prompt 09.5.11C y ejecutar `.\scripts\test_source_quality.ps1 -Mode Full`.
No se pega 09.5.12 hasta que Full sea verde de verdad.

Full de esta sesión: static y dotnet pasaron; `python-tests` falló (exit -1)
con los fallos dueños de voz. No es `source_quality_gate_passed: mode=Full`.
