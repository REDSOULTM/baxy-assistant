# Ambiente — 09.5.11C Revalidar Goals 07–09

Cerrado. El FALLO_DE_AMBIENTE de `silero_vad`/`sounddevice` en `py -3.12`
ya no aplica. Full corrió sobre HEAD limpio `b5820a1` y pasó.

`next_human_prompt` permanece en
`documentacion/sprints/09.5.12_INTEGRAR_Y_REPLANIFICAR.md`.

## Captura COM

`AudioDucker.duck()` importaba `comtypes` fuera del `try`. Traceback
reproducido: `voice_aec.py` `_com_apartment` → `voice.py:2515`
`ModuleNotFoundError`. Ducking es opcional; el `try` cubre el apartamento
COM. Con `comtypes` oculto: `duck=False` sin excepción.

## Full

```
.\scripts\test_source_quality.ps1 -Mode Full
```

`source_quality_gate_passed: mode=Full` EXIT=0.
dotnet-tests verdes. python-tests: 8755 passed. Owner:
`py -3.12 -m pytest tests/test_goal095_09511c_revalidate.py tests/test_goal09_voice_engines.py -q`
→ 15 passed, 0 failed.

Tests no editados.
