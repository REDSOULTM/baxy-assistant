# Ambiente — 09.5.11C Revalidar Goals 07–09

Verificación independiente que partió de `5 failed, 10 passed`. Traceback
exacto de captura y corrección aplicada. El comando dueño pasó 15/15.
Full no se reejecutó en esta verificación. No se declara el goal cerrado.

`next_human_prompt` permanece en
`documentacion/sprints/09.5.12_INTEGRAR_Y_REPLANIFICAR.md`.

## Traceback exacto (`voice.py:2515`)

```
File src/baxy_mind/voice.py, line 2419, in _capture_loop
    begin_utterance(...)
File src/baxy_mind/voice.py, line 2153, in begin_utterance
    self._ducker.duck()
File src/baxy_mind/voice_aec.py, line 256, in duck
    with self._lock, _com_apartment():
File src/baxy_mind/voice_aec.py, line 227, in _com_apartment
    import comtypes
ModuleNotFoundError: No module named 'comtypes'
```

El `except` de `_capture_loop` (línea 2515) solo registra el tipo, no el
módulo. Causa real: `AudioDucker.duck()` importaba `comtypes` **antes**
del `try` que ya degradaba pycaw ausente. El módulo declara ducking
opcional; la captura no debe morir.

Reproducido ocultando `comtypes`: `duck()` levantaba. Tras el arreglo:
`duck=False` sin excepción. Con `comtypes` presente: `duck_live=True`.

## Codeswitch `nootpad`

`engine.transcribe_pcm("abre notepad please")` → `abre nootpad, please.`
sin `rapidfuzz`. El corrector ya se desactiva con warning; no se relajó
la aserción. En este `py -3.12` está `rapidfuzz==3.14.5`.

## Intérprete dueño

`py -3.12` → Python 3.12.10. No se usó `BAXYRuntime` para sustituirlo.

Presentes: `silero_vad 6.2.1`, `sounddevice 0.5.6`, `comtypes 1.4.16`,
`pycaw`, `rapidfuzz 3.14.5`, `torchaudio 2.7.0`.

## Comando dueño (esta verificación)

```powershell
py -3.12 -m pytest tests/test_goal095_09511c_revalidate.py tests/test_goal09_voice_engines.py -q
```

`15 passed in 46.70s`. Tests no editados.
