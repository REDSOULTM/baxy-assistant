commit 32e3f85e5b922d6a3214351cd3c6c4f1e62f27e4

C03: a reply that names the real destination and denies the requested one is not a wrong-destination claim (DISCORD1869 H0394: «Le envié "hola" a Violeta, no a ShooterCock» was vetoed although it is exactly the truth, and the case ended with no final)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>


diff --git a/src/baxy_mind/llm.py b/src/baxy_mind/llm.py
index daf289d4f..5dc3c4295 100644
--- a/src/baxy_mind/llm.py
+++ b/src/baxy_mind/llm.py
@@ -7551,6 +7551,12 @@ def compose_visible_defect(
                 + re.escape(_accent_folded_with_punctuation(requested)),
                 folded_reply,
             )
+            # DISCORD1869 H0394: «Le envié "hola" a Violeta, no a ShooterCock» names
+            # the real destination and denies the requested one, which is the truth;
+            # only a reply that does not deny it claims the wrong delivery.
+            and not _denies_the_destination(
+                folded_reply, _accent_folded_with_punctuation(requested),
+            )
         ):
             return "sent_wrong_destination"
     if (
