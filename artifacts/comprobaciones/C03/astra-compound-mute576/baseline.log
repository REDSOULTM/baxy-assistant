FFFF..FFFF....F......F..F.                                               [100%]
================================== FAILURES ===================================
_ test_current_questions_conserve_all_requested_reads[Dime la hora y si est\xe1 silenciado el sonido.-operations0] _

text = 'Dime la hora y si está silenciado el sonido.'
operations = ('system.time', 'audio.status')

    @pytest.mark.parametrize("text,operations", [
        ("Dime la hora y si está silenciado el sonido.", ("system.time", "audio.status")),
        ("What time is it, and is the sound on mute?", ("system.time", "audio.status")),
        ("Dime la fecha y dime si el audio está silenciado.", ("system.time", "audio.status")),
        ("Decime la hora y si el volumen está en silencio.", ("system.time", "audio.status")),
        ("¿Está silenciado el sonido y qué hora es?", ("audio.status", "system.time")),
        ("Is the sound muted, and what time is it?", ("audio.status", "system.time")),
        ("Dime si está silenciado el audio y qué hora es.", ("audio.status", "system.time")),
        ("Check if the sound is muted and what time is it.", ("audio.status", "system.time")),
        ("Dime si el sonido está silenciado.", ("audio.status",)),
        ("Comprueba si el audio está silenciado ahora.", ("audio.status",)),
        ("Check whether the sound is on mute.", ("audio.status",)),
    ])
    def test_current_questions_conserve_all_requested_reads(text, operations):
        result = resolve_explicit_effects(text, AVAILABLE)
>       assert result is not None
E       assert None is not None

tests\test_c03_compound_mute_query.py:24: AssertionError
_ test_current_questions_conserve_all_requested_reads[What time is it, and is the sound on mute?-operations1] _

text = 'What time is it, and is the sound on mute?'
operations = ('system.time', 'audio.status')

    @pytest.mark.parametrize("text,operations", [
        ("Dime la hora y si está silenciado el sonido.", ("system.time", "audio.status")),
        ("What time is it, and is the sound on mute?", ("system.time", "audio.status")),
        ("Dime la fecha y dime si el audio está silenciado.", ("system.time", "audio.status")),
        ("Decime la hora y si el volumen está en silencio.", ("system.time", "audio.status")),
        ("¿Está silenciado el sonido y qué hora es?", ("audio.status", "system.time")),
        ("Is the sound muted, and what time is it?", ("audio.status", "system.time")),
        ("Dime si está silenciado el audio y qué hora es.", ("audio.status", "system.time")),
        ("Check if the sound is muted and what time is it.", ("audio.status", "system.time")),
        ("Dime si el sonido está silenciado.", ("audio.status",)),
        ("Comprueba si el audio está silenciado ahora.", ("audio.status",)),
        ("Check whether the sound is on mute.", ("audio.status",)),
    ])
    def test_current_questions_conserve_all_requested_reads(text, operations):
        result = resolve_explicit_effects(text, AVAILABLE)
        assert result is not None
>       assert result.operations == operations
E       AssertionError: assert ('audio.status',) == ('system.time...audio.status')
E         
E         At index 0 diff: 'audio.status' != 'system.time'
E         Right contains one more item: 'audio.status'
E         Use -v to get more diff

tests\test_c03_compound_mute_query.py:25: AssertionError
_ test_current_questions_conserve_all_requested_reads[Dime la fecha y dime si el audio est\xe1 silenciado.-operations2] _

text = 'Dime la fecha y dime si el audio está silenciado.'
operations = ('system.time', 'audio.status')

    @pytest.mark.parametrize("text,operations", [
        ("Dime la hora y si está silenciado el sonido.", ("system.time", "audio.status")),
        ("What time is it, and is the sound on mute?", ("system.time", "audio.status")),
        ("Dime la fecha y dime si el audio está silenciado.", ("system.time", "audio.status")),
        ("Decime la hora y si el volumen está en silencio.", ("system.time", "audio.status")),
        ("¿Está silenciado el sonido y qué hora es?", ("audio.status", "system.time")),
        ("Is the sound muted, and what time is it?", ("audio.status", "system.time")),
        ("Dime si está silenciado el audio y qué hora es.", ("audio.status", "system.time")),
        ("Check if the sound is muted and what time is it.", ("audio.status", "system.time")),
        ("Dime si el sonido está silenciado.", ("audio.status",)),
        ("Comprueba si el audio está silenciado ahora.", ("audio.status",)),
        ("Check whether the sound is on mute.", ("audio.status",)),
    ])
    def test_current_questions_conserve_all_requested_reads(text, operations):
        result = resolve_explicit_effects(text, AVAILABLE)
>       assert result is not None
E       assert None is not None

tests\test_c03_compound_mute_query.py:24: AssertionError
_ test_current_questions_conserve_all_requested_reads[Decime la hora y si el volumen est\xe1 en silencio.-operations3] _

text = 'Decime la hora y si el volumen está en silencio.'
operations = ('system.time', 'audio.status')

    @pytest.mark.parametrize("text,operations", [
        ("Dime la hora y si está silenciado el sonido.", ("system.time", "audio.status")),
        ("What time is it, and is the sound on mute?", ("system.time", "audio.status")),
        ("Dime la fecha y dime si el audio está silenciado.", ("system.time", "audio.status")),
        ("Decime la hora y si el volumen está en silencio.", ("system.time", "audio.status")),
        ("¿Está silenciado el sonido y qué hora es?", ("audio.status", "system.time")),
        ("Is the sound muted, and what time is it?", ("audio.status", "system.time")),
        ("Dime si está silenciado el audio y qué hora es.", ("audio.status", "system.time")),
        ("Check if the sound is muted and what time is it.", ("audio.status", "system.time")),
        ("Dime si el sonido está silenciado.", ("audio.status",)),
        ("Comprueba si el audio está silenciado ahora.", ("audio.status",)),
        ("Check whether the sound is on mute.", ("audio.status",)),
    ])
    def test_current_questions_conserve_all_requested_reads(text, operations):
        result = resolve_explicit_effects(text, AVAILABLE)
>       assert result is not None
E       assert None is not None

tests\test_c03_compound_mute_query.py:24: AssertionError
_ test_current_questions_conserve_all_requested_reads[Dime si est\xe1 silenciado el audio y qu\xe9 hora es.-operations6] _

text = 'Dime si está silenciado el audio y qué hora es.'
operations = ('audio.status', 'system.time')

    @pytest.mark.parametrize("text,operations", [
        ("Dime la hora y si está silenciado el sonido.", ("system.time", "audio.status")),
        ("What time is it, and is the sound on mute?", ("system.time", "audio.status")),
        ("Dime la fecha y dime si el audio está silenciado.", ("system.time", "audio.status")),
        ("Decime la hora y si el volumen está en silencio.", ("system.time", "audio.status")),
        ("¿Está silenciado el sonido y qué hora es?", ("audio.status", "system.time")),
        ("Is the sound muted, and what time is it?", ("audio.status", "system.time")),
        ("Dime si está silenciado el audio y qué hora es.", ("audio.status", "system.time")),
        ("Check if the sound is muted and what time is it.", ("audio.status", "system.time")),
        ("Dime si el sonido está silenciado.", ("audio.status",)),
        ("Comprueba si el audio está silenciado ahora.", ("audio.status",)),
        ("Check whether the sound is on mute.", ("audio.status",)),
    ])
    def test_current_questions_conserve_all_requested_reads(text, operations):
        result = resolve_explicit_effects(text, AVAILABLE)
>       assert result is not None
E       assert None is not None

tests\test_c03_compound_mute_query.py:24: AssertionError
_ test_current_questions_conserve_all_requested_reads[Check if the sound is muted and what time is it.-operations7] _

text = 'Check if the sound is muted and what time is it.'
operations = ('audio.status', 'system.time')

    @pytest.mark.parametrize("text,operations", [
        ("Dime la hora y si está silenciado el sonido.", ("system.time", "audio.status")),
        ("What time is it, and is the sound on mute?", ("system.time", "audio.status")),
        ("Dime la fecha y dime si el audio está silenciado.", ("system.time", "audio.status")),
        ("Decime la hora y si el volumen está en silencio.", ("system.time", "audio.status")),
        ("¿Está silenciado el sonido y qué hora es?", ("audio.status", "system.time")),
        ("Is the sound muted, and what time is it?", ("audio.status", "system.time")),
        ("Dime si está silenciado el audio y qué hora es.", ("audio.status", "system.time")),
        ("Check if the sound is muted and what time is it.", ("audio.status", "system.time")),
        ("Dime si el sonido está silenciado.", ("audio.status",)),
        ("Comprueba si el audio está silenciado ahora.", ("audio.status",)),
        ("Check whether the sound is on mute.", ("audio.status",)),
    ])
    def test_current_questions_conserve_all_requested_reads(text, operations):
        result = resolve_explicit_effects(text, AVAILABLE)
>       assert result is not None
E       assert None is not None

tests\test_c03_compound_mute_query.py:24: AssertionError
_ test_current_questions_conserve_all_requested_reads[Dime si el sonido est\xe1 silenciado.-operations8] _

text = 'Dime si el sonido está silenciado.', operations = ('audio.status',)

    @pytest.mark.parametrize("text,operations", [
        ("Dime la hora y si está silenciado el sonido.", ("system.time", "audio.status")),
        ("What time is it, and is the sound on mute?", ("system.time", "audio.status")),
        ("Dime la fecha y dime si el audio está silenciado.", ("system.time", "audio.status")),
        ("Decime la hora y si el volumen está en silencio.", ("system.time", "audio.status")),
        ("¿Está silenciado el sonido y qué hora es?", ("audio.status", "system.time")),
        ("Is the sound muted, and what time is it?", ("audio.status", "system.time")),
        ("Dime si está silenciado el audio y qué hora es.", ("audio.status", "system.time")),
        ("Check if the sound is muted and what time is it.", ("audio.status", "system.time")),
        ("Dime si el sonido está silenciado.", ("audio.status",)),
        ("Comprueba si el audio está silenciado ahora.", ("audio.status",)),
        ("Check whether the sound is on mute.", ("audio.status",)),
    ])
    def test_current_questions_conserve_all_requested_reads(text, operations):
        result = resolve_explicit_effects(text, AVAILABLE)
>       assert result is not None
E       assert None is not None

tests\test_c03_compound_mute_query.py:24: AssertionError
_ test_current_questions_conserve_all_requested_reads[Comprueba si el audio est\xe1 silenciado ahora.-operations9] _

text = 'Comprueba si el audio está silenciado ahora.'
operations = ('audio.status',)

    @pytest.mark.parametrize("text,operations", [
        ("Dime la hora y si está silenciado el sonido.", ("system.time", "audio.status")),
        ("What time is it, and is the sound on mute?", ("system.time", "audio.status")),
        ("Dime la fecha y dime si el audio está silenciado.", ("system.time", "audio.status")),
        ("Decime la hora y si el volumen está en silencio.", ("system.time", "audio.status")),
        ("¿Está silenciado el sonido y qué hora es?", ("audio.status", "system.time")),
        ("Is the sound muted, and what time is it?", ("audio.status", "system.time")),
        ("Dime si está silenciado el audio y qué hora es.", ("audio.status", "system.time")),
        ("Check if the sound is muted and what time is it.", ("audio.status", "system.time")),
        ("Dime si el sonido está silenciado.", ("audio.status",)),
        ("Comprueba si el audio está silenciado ahora.", ("audio.status",)),
        ("Check whether the sound is on mute.", ("audio.status",)),
    ])
    def test_current_questions_conserve_all_requested_reads(text, operations):
        result = resolve_explicit_effects(text, AVAILABLE)
>       assert result is not None
E       assert None is not None

tests\test_c03_compound_mute_query.py:24: AssertionError
_ test_missing_read_never_authorizes_only_the_recognized_subset[available1-What time is it, and is the sound on mute?] _

text = 'What time is it, and is the sound on mute?'
available = {'audio.status'}

    @pytest.mark.parametrize("text", [
        "Dime la hora y si está silenciado el sonido.",
        "What time is it, and is the sound on mute?",
    ])
    @pytest.mark.parametrize("available", [{"system.time"}, {"audio.status"}])
    def test_missing_read_never_authorizes_only_the_recognized_subset(text, available):
>       assert resolve_explicit_effects(text, available) is None
E       AssertionError: assert EffectIntent(operations=('audio.status',), evidence=('what time is it, and is the sound on mute',)) is None
E        +  where EffectIntent(operations=('audio.status',), evidence=('what time is it, and is the sound on mute',)) = resolve_explicit_effects('What time is it, and is the sound on mute?', {'audio.status'})

tests\test_c03_compound_mute_query.py:34: AssertionError
_ test_conditional_past_and_other_device_requests_do_not_become_current_reads[What time is it and was the sound muted yesterday?] _

text = 'What time is it and was the sound muted yesterday?'

    @pytest.mark.parametrize("text", [
        "Dime la hora y si está silenciado el sonido, apaga el PC.",
        "Dime si está silenciado el sonido y si lo está, abre Steam.",
        "Check if the sound is muted and if it is, open Steam.",
        "Si está silenciado el sonido, abre Steam.",
        "If the sound is muted, shut down the computer.",
        "Dime la hora y si estaba silenciado el sonido ayer.",
        "What time is it and was the sound muted yesterday?",
        "Dime la hora y si está silenciado el televisor.",
        "What time is it and is the microphone muted?",
    ])
    def test_conditional_past_and_other_device_requests_do_not_become_current_reads(text):
>       assert resolve_explicit_effects(text, AVAILABLE) is None
E       AssertionError: assert EffectIntent(operations=('audio.status',), evidence=('what time is it and was the sound muted yesterday',)) is None
E        +  where EffectIntent(operations=('audio.status',), evidence=('what time is it and was the sound muted yesterday',)) = resolve_explicit_effects('What time is it and was the sound muted yesterday?', {'app.open', 'audio.mute', 'audio.status', 'audio.volume', 'note.create', 'system.power', ...})

tests\test_c03_compound_mute_query.py:49: AssertionError
_ test_quoted_questions_stay_note_content[Crea una nota titulada Audio con el texto "la hora y si est\xe1 silenciado el sonido"] _

text = 'Crea una nota titulada Audio con el texto "la hora y si está silenciado el sonido"'

    @pytest.mark.parametrize("text", [
        'Crea una nota titulada Audio con el texto "la hora y si está silenciado el sonido"',
        'Create a note titled Audio with the text "time and is the sound on mute"',
    ])
    def test_quoted_questions_stay_note_content(text):
        result = resolve_explicit_effects(text, AVAILABLE)
>       assert result is not None
E       assert None is not None

tests\test_c03_compound_mute_query.py:58: AssertionError
=========================== short test summary info ===========================
FAILED tests/test_c03_compound_mute_query.py::test_current_questions_conserve_all_requested_reads[Dime la hora y si est\xe1 silenciado el sonido.-operations0]
FAILED tests/test_c03_compound_mute_query.py::test_current_questions_conserve_all_requested_reads[What time is it, and is the sound on mute?-operations1]
FAILED tests/test_c03_compound_mute_query.py::test_current_questions_conserve_all_requested_reads[Dime la fecha y dime si el audio est\xe1 silenciado.-operations2]
FAILED tests/test_c03_compound_mute_query.py::test_current_questions_conserve_all_requested_reads[Decime la hora y si el volumen est\xe1 en silencio.-operations3]
FAILED tests/test_c03_compound_mute_query.py::test_current_questions_conserve_all_requested_reads[Dime si est\xe1 silenciado el audio y qu\xe9 hora es.-operations6]
FAILED tests/test_c03_compound_mute_query.py::test_current_questions_conserve_all_requested_reads[Check if the sound is muted and what time is it.-operations7]
FAILED tests/test_c03_compound_mute_query.py::test_current_questions_conserve_all_requested_reads[Dime si el sonido est\xe1 silenciado.-operations8]
FAILED tests/test_c03_compound_mute_query.py::test_current_questions_conserve_all_requested_reads[Comprueba si el audio est\xe1 silenciado ahora.-operations9]
FAILED tests/test_c03_compound_mute_query.py::test_missing_read_never_authorizes_only_the_recognized_subset[available1-What time is it, and is the sound on mute?]
FAILED tests/test_c03_compound_mute_query.py::test_conditional_past_and_other_device_requests_do_not_become_current_reads[What time is it and was the sound muted yesterday?]
FAILED tests/test_c03_compound_mute_query.py::test_quoted_questions_stay_note_content[Crea una nota titulada Audio con el texto "la hora y si est\xe1 silenciado el sonido"]
11 failed, 15 passed in 0.75s
