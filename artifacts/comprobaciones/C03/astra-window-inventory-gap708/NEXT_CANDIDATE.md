# Candidato de implementación después de705+712

El siguiente ensayo ampliará la lectura existente `window.resolve` con una selección global explícita, manteniendo una sola operación. La opción a contrastar es `process="*"`, con `byTitle=false`; un título `"*"` con `byTitle=true` conserva su significado literal. `process` sigue siendo obligatorio: un argumento omitido o vacío nunca se convierte en una lectura global. El catálogo debe describir esta semántica antes de que se mida el selector. No hay cambio de código ni adopción todavía.

La normalización actual retira `.exe` (`WindowsWindowControlProvider.cs:462–476`). Por eso el selector global debe reconocerse explícitamente antes de normalizar: `"*.exe"`, patrones parciales y nombres no encontrados no pueden adquirir autoridad global por accidente. Se conservarán las búsquedas por proceso/título y la verificación de identidad de cada ventana devuelta.

Para los resultados, heredar la forma de paginación ya usada por notas: cantidad devuelta, total del inventario observado, límite, posición y siguiente página. `NoteHandlers.cs:245–285` y `CoreNotesEndToEndTests.cs:820–875` son el precedente concreto. No copiar su presunción de almacenamiento estable: las ventanas pueden cambiar entre lecturas. No se añade caché persistente ni se afirma una fotografía atómica entre páginas.

El provider debe distinguir fin de enumeración, parada deliberada y error de EnumWindows, además de las ventanas que no pudo observar. Si hubo observación parcial, un total universal no está demostrado. El formato y los validadores de prosa deben conservar esa diferencia; `count=windows.Length` sólo cuenta la página. El precedente de procesos (`ProcessListHandler.cs:38–63,96–139`, `WindowsProcessStatusProvider.cs:20–95`) distingue observados y devueltos, pero no acredita un inventario completo y no basta por sí solo.

Validación del próximo conjunto: lecturas globales y filtradas en español/inglés/mezcla, nombres/títulos cambiantes, mayúsculas, selector vacío, patrones no globales, cero ventanas, varias páginas, límite exacto, ventana desaparecida, error de enumeración, identidad y cancelación. Comprobar consultas de cantidad frente a solicitudes de nombres y referencias de continuidad. Un inventario global no autoriza a cerrar ni modificar todas las ventanas. Se mantienen las garantías del kernel y los controles negativos de alcance.

La ruta Python actual sólo deriva un título literal para window.resolve (`__main__.py:4243–4245`) y el filtro de dominio no acredita compatibilidad completa (`effect_intent.py:1901–1905`). Primero disponer de la capacidad real; después medir propuesta nativa, argumentos y primera transformación errónea, aprovechando la recuperación fuerte existente. La sonda707 y el verificador débil rechazado no se promueven. Las pruebas dueñas acompañarán cada cambio; el Full se ejecutará antes de adoptar el conjunto C#+Python, no por cada experimento aislado.

Este documento decide qué candidato implementar y medir, no que ya cumpla. La adopción requerirá resultado real y regresión del producto. La encuesta permanece26/716/0; siguiente acción inmediata sigue siendo terminar Full5 y producto706.
