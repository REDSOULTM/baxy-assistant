--- upstream/bindings/webrtc_audio_bindings.cpp
+++ diagnostic/bindings/webrtc_audio_bindings.cpp
@@ -355,6 +355,7 @@
 class EchoCanceller {
     std::unique_ptr<webrtc::EchoControl> aec_;
     std::unique_ptr<webrtc::AudioBuffer> near_buf_;
+    std::unique_ptr<webrtc::AudioBuffer> linear_buf_;
     std::unique_ptr<webrtc::AudioBuffer> far_buf_;
     std::unique_ptr<webrtc::HighPassFilter> hp_filter_;
     int sample_rate_;
@@ -400,7 +401,7 @@
         near_buf_->SplitIntoFrequencyBands();
         hp_filter_->Process(near_buf_.get(), true);
         aec_->SetAudioBufferDelay(stream_delay_ms_);
-        aec_->ProcessCapture(near_buf_.get(), nullptr, false);
+        aec_->ProcessCapture(near_buf_.get(), linear_buf_.get(), false);
         near_buf_->MergeFrequencyBands();
     }
 
@@ -424,11 +425,14 @@
         frame_stride_ = frame_size_ * num_channels;
 
         webrtc::EchoCanceller3Config config;
+        config.filter.export_linear_aec_output = true;
         webrtc::EchoCanceller3Factory factory(config);
         aec_ = factory.Create(sample_rate, num_channels, num_channels);
 
         hp_filter_ = std::make_unique<webrtc::HighPassFilter>(
             sample_rate, num_channels);
+
+        linear_buf_ = std::make_unique<webrtc::AudioBuffer>(16000, num_channels, 16000, num_channels, 16000, num_channels);
 
         near_buf_ = std::make_unique<webrtc::AudioBuffer>(
             sample_rate, num_channels,
@@ -513,10 +517,27 @@
 
     void reset() {
         webrtc::EchoCanceller3Config config;
+        config.filter.export_linear_aec_output = true;
         webrtc::EchoCanceller3Factory factory(config);
         aec_ = factory.Create(sample_rate_, num_channels_, num_channels_);
         hp_filter_ = std::make_unique<webrtc::HighPassFilter>(
             sample_rate_, num_channels_);
+    }
+
+    py::array_t<float> last_linear_frame() const {
+        py::array_t<float> output(160 * num_channels_);
+        buffer_to_float(linear_buf_.get(), 160, num_channels_, output.mutable_data());
+        return output;
+    }
+
+    py::dict diagnostic_metrics() const {
+        auto values = aec_->GetMetrics();
+        py::dict result;
+        result["echo_return_loss"] = values.echo_return_loss;
+        result["echo_return_loss_enhancement"] = values.echo_return_loss_enhancement;
+        result["delay_ms"] = values.delay_ms;
+        result["active"] = aec_->ActiveProcessing();
+        return result;
     }
 
     int get_stream_delay_ms() const { return stream_delay_ms_; }
@@ -1153,6 +1174,8 @@
         .def("process", &EchoCanceller::process,
              py::arg("near"), py::arg("far"))
         .def("reset", &EchoCanceller::reset)
+        .def("last_linear_frame", &EchoCanceller::last_linear_frame)
+        .def("diagnostic_metrics", &EchoCanceller::diagnostic_metrics)
         .def_property("stream_delay_ms",
                        &EchoCanceller::get_stream_delay_ms,
                        &EchoCanceller::set_stream_delay_ms);
