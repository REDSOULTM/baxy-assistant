# G02.09 — registro prueba por prueba (C02)

| Prueba | Qué cambió y por qué |
|---|---|
| `test_descriptor_does_not_depend_on_the_sibling_baxy_worktree` | Sustituye el pin a `${REPOSITORY_ROOT}\legacy`. El descriptor no puede arrastrar el hermano. |
| `test_llama_server_resolution_does_not_select_the_sibling_baxy_worktree` | `resolve_asset("llama_server")` no puede devolver un path bajo `Programacion\BAXY`. |
| `test_development_launcher_delegates_runtime_hash_verification_to_app` | Añade `IsForeignBaxyWorktree`, `foreign_baxy_worktree` en register, y que el ViewModel ya no define `HandlePendingMindPlanAsync` / `HandlePendingMemoryConfirmationAsync`. |
| `RegisteredRuntimeRejectsASwappedLlamaServerHash` | Un llama-server distinto del SHA declarado pone LoadRegistered en null. |
| `RegisteredRuntimeRejectsTheForeignBaxyWorktreeEvenWhenHashesMatch` | Path `\BAXY\legacy\` se rechaza aunque el hash cuadre. |
| `ForeignBaxyWorktreeDetectsTheExactSiblingSegment` | `BAXY Definitivo` y `BAXYRuntime` no son el hermano. |
| `MissionEngineTests.ExposesASinglePublicConstructor` | Un constructor público: registry, journal, options opcional. |
| `MindPlanSessionTests` | Cancelar sin efecto incierto limpia el plan; réplica no reconocida pide continuar/cancelar sin Core. Conducta extraída, no un rename. |
| `MemoryTurnSessionTests` | Identidad de operación; confirmación ausente falla cerrado. |
| `test_stt_quality_evaluators` | Sello vivo = árbol actual; pin `08300d7c` histórico de la auditoría; preregister minds14 no se reescribe. |
| `test_wakeword_runtime_resources` | Sin cambio de aserción; se conserva (identidad SessionOptions ≠ CPU). |
| FieldUi `FieldSourceAndRebuiltPayloadMatchTheCurrentSeal` | Sin cambio de constante; decisión en ORIGIN.md. |
| R281 `test_local_manifest_matches_the_published_expectation` | Hashes GGUF/llama/python siguen; sólo cambió la ruta de llama-server. |
| `asset_resolver.ps1` required_files | StrictMode no revienta directorios opcionales (TTS) al registrar. |

Ningún rojo se cerró con skip/xfail/umbral relajado. No se borraron pruebas ajenas.

## Revalidación 2026-09-04 (HEAD `f9391c1`)

| Prueba | Qué se comprobó |
|---|---|
| `test_asset_resolution.py` | 8 pass; descriptor y llama-server sin hermano. |
| Integration Discovery/Plan/Memory/FieldUi | 26 pass; sello `0F6DCDA5…` sin subir la constante. |
| `MissionEngineTests` | 17 pass; un constructor. |
| `test_stt_quality_evaluators.py` | 12 pass / 1 skip env. Sello vivo `651f6989` (C03 tocó `goal06_voice_sample.py`). Pin histórico `08300d7c` y preregister minds14 no reescritos. |
| Full ×2 + clon | gate_passed; 0 fail; 0 warn; clon 11 skip env vs 3 desarrollo. |
| Conductor | C01 en implementación ×2 y clon ×1; `system.time` verified vs reloj de pared. |

WIP C03 no se publica aquí. Hermano `203c34a` no modificado.
