# CLAUDE_CONTEXT_CARTER_REQUIREMENTS.md
# Auditoría de ContextoCarter.md — Requisitos vs. Código Real
# Generado por: Claude Code (claude-sonnet-4-6) — 2026-05-06
# FUENTE DE VERDAD: ContextoCarter.md (944 líneas)

---

| # | Requisito | Fuente / Sección | Estado en código | Evidencia | Gap | Riesgo |
|---|---|---|---|---|---|---|
| 1 | Carter es local y privado. No envía datos a nube sin permiso. | Valor 1 | PARCIAL — La arquitectura es local. El adapter Ollama es local. Pero si se usa un adapter cloud (OpenAI compat), no hay bloqueo explícito de datos. | `ollama_adapter.py` existe. `openai_api_adapter.py` también. No hay política que fuerce local. | No hay gate que impida configurar un endpoint cloud. El usuario podría conectar a OpenAI sin saberlo. | MEDIUM |
| 2 | Velocidad: trivial 3-5s máx 8s, tool simple 5-8s máx 12s, apps 5-12s máx 20s | Valor 2 | NO MEDIDO en runtime actual. Los tests de latencia son con `ScriptedAdapter` (sin LLM real). | `test_llm_first_responses.py` usa scripted adapter — no mide latencia real. | No hay medición de latencia live en CI. p95 del JSON de audit no fue validado en esta sesión. | HIGH |
| 3 | No mentir nunca. No decir "listo/hecho/completado" sin verificación. | Valor 3 | IMPLEMENTADO — `fake_success_guard` en `guards.py`. `_DONE_ANCHORS` bloquea claims. Verifier checks antes de CONFIRMED. | `guards.py:100` `fake_success_guard` activa. `test_guards.py` pasa. | Guard funciona para inicio de reply. Claims en medio de respuesta NO están bloqueados. | MEDIUM |
| 4 | Verificación de cada acción con efecto (volumen, apps, filesystem, web). | Valor 4 | IMPLEMENTADO — `VerificationManager` con verifiers por tool. Volume: ±5% tolerance. App: process/window check. File: path+size. | `verifier.py` — cada tool tiene verifier registrado. | Verifier de `notify_toast` usa `synchronous_ok` — solo comprueba `result.ok`, no que la notificación apareció visualmente. | LOW |
| 5 | Fallar bien pero no rendirse fácil. Intentar recuperarse antes de reportar fallo. | Valor 5 | PARCIAL — `recovery.py` existe (`maybe_retry_app_open`). Hay fallbacks en agent loop. | `agent.py:697` fallback synthesis cuando LLM no llamó tool. | Recuperación solo para `app_open`. Para web, terminal, filesystem no hay retry automático. | MEDIUM |
| 6 | Universal — no hardcodes por frase, por app, por idioma. | Valor 6 | IMPLEMENTADO — `hardcode_guard.py` limpio. No hay `if "steam"` en runtime. | `hardcode_guard: clean (58 files scanned)` | `official_matrix_cases.py` tiene `if "steam" in folded_tools:` en el parser de casos — pero ese archivo es el runner, no el runtime. El runtime está limpio. | LOW (runner only) |
| 7 | No hacks por app (Steam, WhatsApp, YouTube, Spotify, etc.). | Valor 7 | IMPLEMENTADO en runtime. | `hardcode_guard.py` DENY_BRANDS_HINT incluye todas las marcas mencionadas. | El runner `official_matrix_cases.py` sí menciona "steam" en parsing de guía. Aceptable porque es parsing del documento, no routing de runtime. | LOW |
| 8 | Multi-modelo: adaptarse a hardware con perfiles por VRAM. | Valor 8 | PARCIAL — `models/selector.py` existe, `models/registry.py` existe. Perfiles definidos. | Perfiles en `registry.py`. | No hay configuración de perfil activo en `pyproject.toml`. El perfil se selecciona en tiempo de ejecución pero no hay test que cubra el cambio de perfil real. | LOW |
| 9 | Protocolo correcto por modelo sin `if model == "..."`. | Valor 9 | IMPLEMENTADO — `ToolCallMode` declarativo. `llm_protocol.py` abstrae el modo. | `adapters/llm_protocol.py` — declarativo, sin `if model_name ==`. | No hay test de modelo con `ToolCallMode.NONE` fallback real (solo scripted). | LOW |
| 10 | Núcleo texto perfecto antes de voz/cámara. | Valor 10 | PARCIAL — 490 tests pasan pero hay gaps importantes: latencia no medida live, algunas categorías débiles. | Pytest 490/490. | Ver gaps en CLAUDE_CATEGORY_CAPABILITY_AUDIT.md. | HIGH |
| 11 | Responder bien a inputs triviales (a, ok, qué?, nada, jaja). Sin activar tools. | Valor 11 | IMPLEMENTADO — `IntentClassifier` detecta `trivial_lowinfo`. No activa tools para esos inputs. | `intent_classifier.py:68-77`. `test_runtime_persona_and_capabilities.py` incluye estos casos. | `"a"` y mono-token <= 4 chars van a `trivial_lowinfo`. "gracias" tiene 7 chars y puede ir a `potential_action` si el resolver lo clasifica como verbo. | MEDIUM |
| 12 | No contaminación por ventana activa en conversación simple/identidad. | Valor 12 | IMPLEMENTADO — `active_app_contamination_guard` en guards. `collect_active_app_tokens` solo cuando `looks_action=True`. | `guards.py:188` `active_app_contamination_guard`. | La guard solo verifica tokens en la *reply*. Si el prompt del sistema incluye active_window en contextos conversacionales, eso no se guarda. | MEDIUM |
| 13 | GUI/visión solo on-demand. No para "hola", "qué?", "gracias". | Valor 13 | IMPLEMENTADO — `PerceptionRouter` con `PerceptionLevel`. `select_tools` filtra GUI tools por nivel. | `perception/ladder.py`. `turn_support.py:select_tools`. | GUI tools (`gui_click`, `gui_type`) solo aparecen cuando `perception.level` lo permite. Sin embargo, `desktop_screenshot` puede aparecer incluso con percepción baja si el LLM la solicita. | LOW |
| 14 | Memoria limpia — solo guardar lo útil, no eco verbatim, no datos sensibles. | Valor 14 | IMPLEMENTADO — `memory_echo_guard`. `secret_filter`. `declarative_detector` con heurísticas. | `guards.py:251` memory_echo_guard. `memory/store.py` con secret filter. | `declarative_detector` usa `_COPULA_ANCHORS` y `_FIRST_PERSON_HINTS` en allowlist. La detección es heurística, no perfecta. Puede perder datos no-declarativos. | LOW |
| 15 | Seguridad — pedir confirmación/bloquear acciones destructivas. | Valor 15 | IMPLEMENTADO — `PolicyEngine` con `_DANGEROUS_PATTERNS` y `_EXFIL_PATTERNS`. `RiskLevel.HIGH/CRITICAL` requieren approval. | `security/policy.py` — 20+ patterns. | Patrones de shutdown incluyen `reinicia el pc` pero no `reboot` en solitario. Algunos combos no cubiertos. Ver CLAUDE_RUNTIME_CODE_AUDIT.md. | MEDIUM |
| 16 | Misiones compuestas con progreso por paso, verificación intermedia. | Valor 16 | PARCIAL — `step_budget=6` en agent. Compound intent detectado. Pero no hay progress reporting durante la misión. | `agent.py:644` — loop over steps hasta step_budget. | No hay emit de progreso parcial al usuario DURANTE la misión. Solo resultado final. Para misiones largas el usuario no ve nada hasta que termina. | HIGH |
| 17 | Transparencia de progreso — no silencio largo. | Valor 17 | NO IMPLEMENTADO — No hay streaming de progreso. Carter responde solo al final. | Ninguno. | Carter no tiene mecanismo de "buscando...", "verificando...", "abriendo..." durante ejecución. Esto es un gap real de UX. | HIGH |
| 18 | Trazabilidad interna completa (intención, tool elegida, por qué, contexto, tiempo). | Valor 18 | IMPLEMENTADO — `TurnTrace` con `emit_state` y `emit`. `last_turn_trace` accesible. | `trace.py`, `agent.py:last_turn_trace`. | La traza existe internamente pero no hay forma de que el usuario la consulte fácilmente desde CLI. No es un gap funcional. | LOW |
| 19 | Adaptar a forma de hablar: español informal, inglés, typos, mezclas, frustración. | Valor 19 | PARCIAL — multilingüe implementado, typos tolerados por fuzzy matching del resolver. | `resolvers/language_detect.py`, `resolvers/resource_resolver.py` con fuzzy. | Typos en nombres de apps: si el score de fuzzy es < 70, Carter no resuelve. "stean" debería resolver a "steam" pero depende del threshold y la lista de apps instaladas. | MEDIUM |
| 20 | Distinguir conversación de acción. "por qué eres inútil" no es GUI. | Valor 20 | IMPLEMENTADO — `IntentClassifier`. "por qué eres inútil" no activa tools (no ends con ?, no compound, no imperative claro). | `intent_classifier.py` + `looks_imperative` + `looks_action` derivation en `agent.py`. | "por qué" va a `question` → `looks_action=False`. Correcto. Pero algunos verbos en español terminan en IMPERATIVE_SUFFIXES aunque sean preguntas con inversión de sintaxis. | LOW |
| 21 | Personalidad directa, honesta, útil, tranquila. Se disculpa si falla. | Valor 21 | PARCIAL — El LLM genera la respuesta. El sistema prompt tiene "Reply briefly and honestly". No hay garantía de personalidad específica. | `turn_support.py:build_messages` — persona string. | La persona depende del modelo. Con modelos distintos la personalidad varía. No hay test de "disculpas" auténticas. | LOW |
| 22 | Cuidar recursos del sistema — no crashear por cargar modelos extra. | Valor 22 | NO IMPLEMENTADO — No hay monitoreo activo de RAM/VRAM en runtime. `system_get_gpu_info` puede leer VRAM pero no hay gate que lo use. | `tools/dispatch_system.py` lee GPU info. | No hay lógica que degrade automáticamente si VRAM > 85%. Carter puede intentar cargar un modelo pesado aunque la memoria esté llena. | HIGH |
| 23 | Rollback de cambios importantes (modelo, protocolo, memoria, config). | Valor 23 | PARCIAL — Git provee rollback de código. Memoria tiene `memory_delete`. Pero no hay sistema de rollback formal. | Git tags existen como rollback points. | No hay comando Carter para "vuelve al estado anterior". El rollback es manual vía Git. | MEDIUM |
| 24 | Medirse con pruebas reales, no solo tests bonitos. | Valor 24 | PARCIAL — Tests sintéticos están bien. Live testing con modelo real es manual. | 490 tests pasan. Live runs en audit/runs/. | Los 490 tests usan `ScriptedAdapter` para la mayoría. Los tests con LLM real son el minimum runner y el full matrix runner, que no son parte de CI. | HIGH |
| 25 | Matriz brutal 18 categorías × 30 casos. Cada bug real → caso permanente. | Valor 25 | PARCIAL — Matriz 18×30 existe en `official_matrix_cases.py`. Pero los 540 casos se corren externamente, no en CI. | `audit/official_matrix_cases.py`. | La matriz no corre en `pytest`. Es un runner separado. Los bugs reales del usuario no están todos capturados en `test_live_regressions_from_user_log.py`. | MEDIUM |
| 26 | Preparado para voz futura (sin lógica de acción propia en capa voz). | Valor 26 | PENDIENTE — Sin implementar, correcto. | Fuera de scope actual. | Voz fuera de scope. El núcleo texto está en progreso. | OUT_OF_SCOPE |
| 27 | Preparado para cámara futura (permiso, privacidad, no permanente). | Valor 27 | PENDIENTE — Sin implementar, correcto. | Fuera de scope actual. | Cámara fuera de scope. | OUT_OF_SCOPE |
| 28 | Modular pero no sobreingenierizado. Sin capas innecesarias. | Valor 28 | PARCIAL — 58 archivos Python. La arquitectura es limpia. Pero existen muchos reportes .md acumulados (100+). | Estructura src/ clara. | Hay exceso de archivos de reportes en Carter_v3/. No afecta runtime pero ensucia el proyecto. | LOW |
| 29 | Compañero de PC real — apps, ventanas, volumen, screenshots, archivos, web, terminal. | Valor 29 | IMPLEMENTADO — 32 tools en catalog. Cubre todas las capacidades mencionadas. | `tools/catalog.py` — 32 tools. | Capacidades dependen de modelos disponibles. Con `ToolCallMode.NONE` o modelo débil, las capacidades efectivas son cero. | MEDIUM |
| 30 | Ganar confianza: responde rápido, no miente, no rompe, pregunta cuando debe. | Valor 30 | PARCIAL — La arquitectura apunta a esto. Los gaps de latencia real y progress reporting afectan la confianza. | Verifier, guards, policy todo implementado. | La latencia no está medida en CI. El progress reporting en misiones compuestas no existe. | HIGH |

---

## Resumen de gaps críticos desde ContextoCarter.md

| Prioridad | Gap | Valor afectado |
|---|---|---|
| BLOCKER | Sin medición de latencia en CI (solo scripted, no live) | Valor 2 |
| HIGH | Sin progress reporting durante misiones compuestas | Valores 16, 17 |
| HIGH | Sin degradación automática por recursos (RAM/VRAM) | Valor 22 |
| HIGH | Tests CI con ScriptedAdapter no validan LLM real | Valor 24 |
| MEDIUM | Política de shutdown no cubre todos los casos (ej: reboot solo) | Valor 15 |
| MEDIUM | No hay gate que impida configurar endpoint cloud | Valor 1 |
| MEDIUM | Typo en app-name: threshold fuzzy puede fallar | Valor 19 |
| MEDIUM | Tag HEAD ≠ working tree — código no commiteado | Integridad general |
