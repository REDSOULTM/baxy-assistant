# CLAUDE_RUNTIME_CODE_AUDIT.md
# Auditoría del Runtime Real — Módulos Principales
# Generado por: Claude Code (claude-sonnet-4-6) — 2026-05-06

---

| # | Módulo | Hallazgo | Severidad | Evidencia | Impacto | Fix recomendado |
|---|---|---|---|---|---|---|
| 1 | `agent.py` | Working tree tiene +104 líneas no commiteadas sobre el tag `carter-v3-18x30-true-ready`. El tag no describe el estado actual del código. | BLOCKER | `git diff --stat` — 7 archivos modificados, 202 líneas diferencia | Integridad del tag quebrada. Si Codex trabaja desde el tag, trabaja con código desactualizado. | Commitear el estado actual o crear nuevo tag que lo represente. |
| 2 | `agent.py` | Sin progress reporting durante misiones compuestas. El usuario ve silencio hasta que termina. | HIGH | `agent.py:644` — loop `for step in steps[:budget]` sin emits intermedios al usuario | Viola Valor 17 (transparencia de progreso). Misiones de 5+ pasos pueden tomar 30-60s sin feedback. | Emitir mensajes de progreso tipo "Ejecutando paso 1/3: abriendo X..." entre steps. |
| 3 | `agent.py:_synthesise_direct_tool_request` | La función `_synthesise_direct_tool_request` en `agent.py` (importada via `synthesise_structural_tool_calls` de `request_patterns`) usa `looks_clock_inquiry`, `looks_volume_adjust_request`, etc. Estas funciones tienen regex específicas para patrones de idioma. | MEDIUM | `request_patterns.py:46-65` — `_CLOCK_INQUIRY`, `_VOLUME_ADJUST` con patrones en español/inglés | Si el usuario pregunta la hora en otro idioma (ej. "quelle heure est-il"), no se activa `looks_clock_inquiry`. No es hardcode por app pero sí limitación de idiomas en los helpers. | Expandir patterns o dejar que el LLM maneje la herramienta en lugar de pre-LLM synthesis. |
| 4 | `guards.py:fake_success_guard` | `_DONE_ANCHORS` bloquea solo cuando la respuesta *empieza* con esas palabras. Claims en medio del texto ("lo hice y está listo") no se bloquean. | MEDIUM | `guards.py:106-109` — `low.startswith(anchor)` | Carter puede decir "Intenté abrir Steam, está listo ahora." y pasar el guard si `any_confirmed=False`. | Extender la detección de claims falsos más allá del inicio del texto. |
| 5 | `guards.py` | `_TIME_MENTION_RE` detecta `9 AM` o `9:00` en cualquier reply. Si Carter menciona una hora en contexto conversacional válido (ej. respondiendo "¿qué hora es?"), el guard puede disparar `time_specific_claim_without_confirmation` aunque no haya claim de acción. | MEDIUM | `guards.py:131-137` — `_TIME_MENTION_RE.search(low)` sin verificar si es reply de clock_now | Falso positivo del guard: bloquea respuestas válidas de hora. | Verificar si `any_confirmed` incluye `clock_now` como tool confirmada. El guard ya recibe `any_confirmed` desde verifier outcomes. |
| 6 | `response_composer.py:145-146` | Dead code detectado — unreachable `return` después de `return f"Captura guardada..."`. | LOW | `response_composer.py:145-146` — dos `return` consecutivos, el segundo nunca se ejecuta | No causa bugs, pero indica falta de revisión del código. | Eliminar el return inalcanzable. |
| 7 | `verifier.py:_synchronous_ok` | `notify_toast` usa `synchronous_ok` — solo verifica que `result.ok=True`. No hay readback visual de que la notificación apareció. | LOW | `catalog.py:110` — `notify_toast` con `verifier="synchronous_ok"` | Si el sistema Windows no muestra la notificación (DND activo, permisos), Carter reporta CONFIRMED incorrectamente. | Aceptable como SKIPPED, no como CONFIRMED. Cambiar a `verifier="skipped"` para notify_toast. |
| 8 | `local_reminders.py:parse_due_at` | Solo parsea: ISO datetime, offset relativo (en X minutos/horas), y texto de reloj (HH:MM, H:MM AM/PM). Frases naturales como "pasado mañana a las 3" o "el lunes" o "en un rato" no se parsean. | MEDIUM | `local_reminders.py:196-264` — `_parse_relative_offset`, `_parse_clock_text` | El usuario puede decir "recuérdame mañana" y obtener `unrecognized_time`. Respuesta honesta pero limitante. | Ampliar `_parse_clock_text` para incluir "pasado mañana", días de semana, "en un rato" (≈30min). |
| 9 | `security/policy.py:_DANGEROUS_PATTERNS` | El pattern de shutdown incluye `\bapaga(?:r)?\b.{0,80}\bwindows\b` pero no captura `shutdown` en inglés solitario sin contexto. Si el usuario dice "shutdown the server", puede no bloquear. | MEDIUM | `policy.py:52-60` — pattern de shutdown requiere "pc\|computer\|equipo\|sistema\|system\|windows" opcional | Potencial gap de seguridad para usuarios en inglés. | Agregar pattern `\bshutdown\b` solo como HIGH risk (no CRITICAL), requiriendo confirmación. |
| 10 | `session_state.py:_short_same_language_nonsecret` | La lógica de confirmación de intento pendiente (`pending_intent_candidate`) usa `_short_same_language_nonsecret` que rechaza confirmaciones con mayúsculas después del primer char (`any(ch.isupper() for ch in text[1:])`). "OK" no pasa. "Sí" no pasa (S mayúscula). | MEDIUM | `session_state.py:306-322` | El usuario que confirma con "Sí" o "YES" o "OK" no activa `pending_intent`. Afecta follow-ups en C17. | Relajar la restricción de mayúsculas para afirmativos cortos conocidos ("Sí", "Si", "OK", "YES"). |
| 11 | `request_patterns.py:IMPERATIVE_SUFFIXES` | `IMPERATIVE_SUFFIXES` incluye `"pen"` como sufijo verbal. Palabras como "open" terminan en "pen" y activan `looks_imperative`. Esto puede causar que "open" en cualquier contexto sea clasificado como imperativo. | LOW | `request_patterns.py:121` — `"pen"` en IMPERATIVE_SUFFIXES | Si el usuario escribe algo como "European" podría activar `looks_imperative`. Bajo riesgo en práctica. | Revisar si `"pen"` es necesario o si se puede eliminar sin perder casos. |
| 12 | `turn_support.py:build_messages` | El `system_prompt` dice "if a safe listed tool clearly fits the current turn, call it now instead of asking whether to continue" — esto puede causar que el LLM ejecute tools sin pedir confirmación incluso en casos donde debería preguntar primero. | MEDIUM | `turn_support.py:61-73` | Para misiones ambiguas, el LLM puede ejecutar la tool directamente. La política y el verifier son el backstop, pero el prompt activamente promueve ejecución directa. | Matizar el prompt: "call it now if the intent is clear AND unambiguous AND non-destructive". |
| 13 | `resolvers/intent_classifier.py` | `compound_action` se detecta por glyphs (`->`, `=>`, `;`) o multi-sentence. Pero "Abre Notepad y ciérralo" no tiene glyph y puede no ser compound_action (ya que `looks_sequential_compound_request` requiere `n >= 5` tokens y `looks_imperative` en primer clause). | MEDIUM | `intent_classifier.py:84-100` — `looks_sequential_compound_request` con condición `n >= 5` | "Abre Notepad y ciérralo" tiene 4-5 tokens. Puede ir a `potential_action` en lugar de `compound_action`. | Verificar manualmente con el modelo si este caso llega a compound_action o se resuelve como action simple. |
| 14 | `tools/verifier.py:_app_open` | Con `launch_time` presente, un proceso *preexistente* retorna `PENDING` con `preexisting=True`. El `response_composer.py` maneja esto correctamente. PERO si el usuario abre Steam justo antes de que Carter intente abrirla, Carter no puede confirmar — lo cual es correcto pero puede parecer confuso. | LOW | `verifier.py:217-219` | Honesto pero puede causar confusión. No es un bug, es comportamiento correcto per spec. | Documentar en RESIDUAL.md como comportamiento esperado. |
| 15 | `tools/catalog.py` | No hay tool para `system_set_mute` — hay `system_mute(toggle=True/False)`. Si el LLM intenta llamar `mute` o `unmute` directamente sin el argumento `toggle`, el dispatcher falla silenciosamente o retorna error. | LOW | `catalog.py:103-108` | El LLM debe aprender que mute requiere `toggle: true/false`. Con modelos débiles puede fallar. | Agregar descripción más clara en el ToolSpec de system_mute. |
| 16 | `memory/declarative_detector.py` | Allowlisted en hardcode_guard. Contiene `_COPULA_ANCHORS` y `_FIRST_PERSON_HINTS`. Aunque no es routing semántico de acciones, es una lista de frases en español/inglés que gatillan "guardar en memoria". Si el usuario escribe algo como "soy estudiante" o "trabajo en X", Carter ofrece guardarlo. | LOW | `memory/declarative_detector.py` — allowlisted | El detector es heurístico. Puede disparar en inputs que el usuario no quería guardar. No crítico pero puede ser molesto. | Aceptable. La oferta es optional (pide confirmación). |
| 17 | `perception/ladder.py` | No leído completamente — la lógica de `PerceptionRouter.decide()` afecta cuántos tools recibe el LLM. Si `perception.level == "none"`, el LLM no recibe tools de GUI. Pero el límite entre "none" y "light" puede estar mal calibrado. | MEDIUM | `perception/ladder.py` — no auditado completamente | Si el LLM recibe GUI tools cuando no debería, puede intentar clicks innecesarios. | Auditar `PerceptionLevel` y cuándo se activa. |
| 18 | `adapters/scripted_adapter.py` | Usado en tests CI. No ejercita el LLM real. Los tests con ScriptedAdapter validan el *harness* pero no el comportamiento del *modelo real*. | HIGH | Todos los tests de integración que usan ScriptedAdapter | Los 490 tests no prueban que un LLM real hará las llamadas correctas. Solo que el engine procesa correctamente los tool calls si el LLM los genera bien. | Aceptable como unit/integration coverage. La validación live es responsabilidad del runner externo. |
| 19 | `llm_verbalizer.py` | No auditado directamente. Es el módulo que genera la respuesta final usando el LLM. Si el verbalizer hace una segunda llamada al LLM y ese LLM genera fake success, el fake_success_guard debe estar activo en `run_reply_guards`. | MEDIUM | `turn_support.py:run_reply_guards` — se llama después de verbalize | Si `generate_result_reply` tarda más de `_RESULT_VERBALIZER_LATENCY_GUARD_MS` (10s), hay un fallback. Verificar que el fallback no introduce fake success. | Verificar en spotcheck manual. |
| 20 | `tools/dispatch_app.py` | No leído completamente. El dispatcher de apps es crítico para el flujo app_open/app_close. Contiene lógica de Popen, proceso lookup, y graceful close. | HIGH — PENDIENTE | `tools/dispatch_app.py` — no auditado en esta sesión | Si hay hacks por app en dispatch_app.py, el hardcode_guard debería detectarlos (guard está clean). | Confiar en hardcode_guard clean + hacer spotcheck manual de open/close Notepad. |

---

## Hallazgos estructurales positivos

| Fortaleza | Evidencia |
|---|---|
| Sin hardcodes de marca en runtime | `hardcode_guard: clean (58 files)` |
| Verifier real para cada tool (no solo result.ok) | `verifier.py` — 14 verifiers registrados |
| Política de seguridad con 20+ patterns | `policy.py` — `_DANGEROUS_PATTERNS` + `_EXFIL_PATTERNS` |
| Guard system post-LLM | `guards.py` — 8 guards distintas |
| Memoria con secret filter | `memory/store.py` — `contains_secret` antes de save |
| Fuzzy matching de recursos sin app-specific hacks | `resolvers/resource_resolver.py` |
| Tool catalog declarativo con risk levels | `tools/catalog.py` — 32 tools, ≤32 assert |
| Session state con turn TTL decay | `session_state.py` — `begin_turn()` decrementa TTL |
