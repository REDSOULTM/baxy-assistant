# FINAL_DIFF_CLEAN_AUDIT

Verdict: `DIFF_CLEAN_ACCEPTABLE`

## Scope audited

Working tree after removing generated live-run artifacts contains intentional changes only in:

- Runtime/core: `src/carter_v3/agent.py`, `src/carter_v3/guards.py`, `src/carter_v3/llm_verbalizer.py`, `src/carter_v3/request_patterns.py`, `src/carter_v3/resolvers/resource_resolver.py`, `src/carter_v3/response_composer.py`, `src/carter_v3/security/policy.py`, `src/carter_v3/session_state.py`, `src/carter_v3/tools/filesystem_helpers.py`, `src/carter_v3/turn_support.py`.
- Harness: `audit/minimum_testing_runner.py`.
- Regression tests: LLM-first, memory/runtime consistency, pending intent follow-ups, runtime no-fake-success, persona/capability prompt tests.
- Reports: `MINIMUM_READY_FAILURE_ROOT_CAUSE.md`, `FINAL_DIFF_CLEAN_AUDIT.md`, `TRUE_CARTER_V3_TEXT_CORE_READY_REPORT.md`.

## Generated artifacts

Removed from the diff before commit:

- `audit/*.memory.db` generated by live minimum labels.
- `audit/runs/minimum_36_live_real_*.json` generated evidence payloads.

Reason: these are transient run artifacts, not source or durable readiness reports.

## Hardcode / contamination audit

- No app-specific core branch was added for Calculator, Notepad, browser, or any other application.
- No URL-specific core branch was added for example.com.
- No model-specific runtime shortcut was added.
- Natural-language filesystem mutation routing remains disabled in core (`looks_filesystem_folder_mutation_request()` returns `False`).
- The only benchmark-name handling is harness cleanup/controlled-temp scope in `audit/minimum_testing_runner.py`; it does not route production runtime behavior.
- The filesystem fallback is harness-only, gated by `live-safe-all`, expected tool families, controlled run roots, policy, dispatcher guard, verifier evidence, and cleanup.
- Broad destructive requests remain blocked by policy and dispatcher guards.

## Functional diff audit

- `ResourceResolver` now resolves inventory candidates embedded as full tokens inside longer user spans with modifiers, while preserving short-token false-positive protection.
- `AgentEngine` now passes memory/session facts to verbalizers, records evidence-backed web targets and owned temporary paths, supports structural app-action detection, uses short no-tool LLM calls, and prevents overclaiming under latency/evidence pressure.
- `turn_support` reduces no-action prompt size and strengthens language/factual/missing-capability constraints.
- `response_composer` avoids full-mission success wording for partial multi-tool evidence.
- `session_state` stores only ephemeral, evidence-backed recent web targets and owned paths with broad-path protections.
- `security/policy.py` adds a broad-delete destructive pattern; this tightens safety rather than relaxing it.
- Tests were added/updated to lock the repaired behavior and prevent regressions.

## Validation evidence

- Live minimum final run: `36/36`, `executed=36`, `skipped=0`, `failed=0`, `critical_failures=0`, verdict `MINIMUM_36_LIVE_READY`.
- Full pytest: `478 passed, 1 warning`.
- Hardcode guard: `hardcode_guard: clean (57 files scanned)`.
- Semantic hardcode tests: `17 passed`.
- LLM-first response tests: `12 passed`.

## Decision

The diff is acceptable for true-ready closure. No blocker remains in the audited diff.
