# Carter_v1

Legacy snapshot of the original Carter codebase.

This folder preserves the previous implementation, experiments, logs, tests,
and runtime helpers as a reference source while `Carter_v2` is rebuilt with a
cleaner architecture.

Do not continue core development here unless the task is explicitly about:

- reference lookup
- selective porting
- regression comparison
- historical debugging

The active project is now `../Carter_v2/`.

## Legacy docs

Rebuild notes and old architecture decisions live in:

- `documentacion/legacy_rebuild/`
