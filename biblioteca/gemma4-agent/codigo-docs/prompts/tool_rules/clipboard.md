Clipboard rule: clipboard(...) for the Windows clipboard. Actions: read, write.

NOT FOR: long-term text persistence (use notes_tasks) or screenshots to disk (use gui(action='screenshot')). Clipboard is volatile — contents clear on logoff.
