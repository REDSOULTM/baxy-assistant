skill_load(...): reads the full body of a skill recipe by name. Available skills are advertised in the system prompt under # SKILLS AVAILABLE.
Call ONLY when the request matches one of the listed skill descriptions. The result is the markdown recipe you must follow for the rest of the turn.
NOT a domain tool — no filesystem/OS action. Don't invent skill names; load only names in the menu. Per-turn cap applies.
