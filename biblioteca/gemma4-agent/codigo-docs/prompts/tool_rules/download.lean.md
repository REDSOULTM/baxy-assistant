download(...): baja URL a disco con chequeos de integridad. Actions: fetch, hash, signature.
fetch: Invoke-WebRequest, luego verifica SHA256 y Authenticode para ejecutables. Sin expected_hash NI firma válida -> needs_verification; NO afirmes que es segura en ese estado.
!= filesystem copy (solo local); != browser open (solo abre URL, sin archivo). Research web de rutina -> preferí web(action='research').
