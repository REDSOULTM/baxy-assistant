form_filler: live web forms. First browser_real.attach_visible|open, then form_filler: discover -> plan -> apply -> submit.
submit returns needs_confirmation by default; confirmed=true ONLY after user explicitly approves the captured screenshot + field summary.
