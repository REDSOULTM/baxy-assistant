habit_tracker(...): local habit log (nothing leaves the machine). Actions: status|create|list|log|stats|delete.
For recurring activities to count/streak.
NOT reminder(...) (one-off scheduled notifications), NOT notes_tasks(...) (free-form notes/tasks).
