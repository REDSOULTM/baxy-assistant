fact_check: verify a claim vs sources.
fact_check(action="check", claim=..., sources=[...]) — or just claim=... if content already ingested into knowledge.
LEXICAL ONLY (TF-IDF + cosine + negation heuristic). YOU write the final verdict from the ranked matches; do NOT parrot the heuristic verdict.
