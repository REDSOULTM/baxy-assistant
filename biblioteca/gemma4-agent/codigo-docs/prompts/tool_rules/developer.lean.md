developer(...): project tasks — run tests, lint, format, git status/diff, start/stop dev server.
Mutating git (commit/push) intentionally NOT exposed -> use terminal(...) so the user sees the exact command.
