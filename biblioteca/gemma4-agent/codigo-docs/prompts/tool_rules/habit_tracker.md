Habit-tracker rule: habit_tracker(...) is a local habit log. Actions: status, create, list, log, stats, delete.

Distinct from reminder(...) (one-off scheduled notifications) and from notes_tasks(...) (free-form notes and tasks). habit_tracker is specifically for recurring activities the user wants to count or streak.

Stores entries in local state; nothing leaves the machine.
