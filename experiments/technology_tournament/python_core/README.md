# Python core contender

Run with CPython 3.12:

```powershell
py -3.12 baxy_slice.py --server
```

The process consumes UTF-8 JSONL from standard input and emits one protocol
record for each valid input. It has no third-party runtime dependency.
