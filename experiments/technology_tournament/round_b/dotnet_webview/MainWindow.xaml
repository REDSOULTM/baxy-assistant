<Window x:Class="Baxy.Tournament.RoundB.Dotnet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="BAXY — asistente local"
        AutomationProperties.Name="BAXY — asistente local y privado"
        Width="1280" Height="820" MinWidth="760" MinHeight="560"
        WindowStartupLocation="CenterScreen"
        Background="#08090C">
  <Grid>
    <wv2:WebView2 x:Name="WebView"
                  AutomationProperties.Name="Conversación con BAXY"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch" />
    <Border x:Name="StartupOverlay" Background="#08090C" Panel.ZIndex="2">
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="380">
        <TextBlock Text="BAXY" Foreground="#F2F3F5" FontFamily="Segoe UI Variable Display"
                   FontSize="34" FontWeight="SemiBold" HorizontalAlignment="Center" />
        <TextBlock x:Name="StartupMessage" Text="Iniciando el core local…" Foreground="#8D929F"
                   FontSize="13" Margin="0,12,0,0" TextAlignment="Center" />
      </StackPanel>
    </Border>
  </Grid>
</Window>
