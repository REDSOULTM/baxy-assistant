# Clausura exacta de pytest para CPython 3.12 / Windows x64.
# Este archivo es un input curado: lock_python_dependencies.ps1 lo consume,
# pero sólo genera pylock.test-win-x64.toml. Revisar sus pins deliberadamente.

colorama==0.4.6
iniconfig==2.3.0
packaging==26.2
pluggy==1.6.0
Pygments==2.20.0
pytest==9.1.1
