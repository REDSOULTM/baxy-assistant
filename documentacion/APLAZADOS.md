# Aplazados

Lo que se vio de pasada durante los goals 01–10 y **no se persiguió**, porque
no bloqueaba: fragilidades teóricas, caminos de error que nadie ha recorrido,
«esto podría fallar si…».

No es una lista de pendientes que se ignora. Es la entrada del **goal 11**, que
existe para vaciarla: cada línea acaba arreglada, medida y descartada, o
declarada limitación ambiental con su degradado.

## Cómo se anota

Una línea por hallazgo. Con esto basta:

- **Dónde** — fichero y línea, o el componente.
- **Qué podría pasar** — el fallo concreto, no la categoría.
- **Qué lo dispararía** — la condición que haría que ocurriera de verdad.
- **Goal** que lo vio.

No inviertas tiempo en investigarlo: si lo estás investigando, ya dejó de ser un
aplazado y se convirtió en trabajo. Anótalo en una línea y sigue.

Si al anotarlo te das cuenta de que **sí bloquea** —de que alguien lo va a ver
mañana usando BAXY con normalidad— entonces no es de aquí: arréglalo en tu
goal.

## Anotaciones

<!-- Una línea por hallazgo. Formato libre, pero que se entienda sin contexto. -->

- **`src/Baxy.App/MainWindowViewModel.cs`** — quedan ~1.050 líneas de máquina de estados del turno (ejecución de plan multipaso + flujo de confirmación de memoria) sin descomponer, porque comparten `_pendingMindPlan`, `_planStore` y `AddMessage`; separarlas pide un colaborador con estado, no una extracción. No bloquea: compila y pasa. *Goal 01.*
- **`%LOCALAPPDATA%\BAXYRuntime\mind-runtime-v1.json`** — el runtime apunta a `Programacion\BAXY\legacy\models\artifacts\` para `llama_server`, o sea al repositorio del cuarto intento; si alguien borra o mueve `BAXY`, este repositorio se queda sin motor de inferencia y sin wake word. Lo dispararía cualquier limpieza de la carpeta `Programacion`. **El goal 03 cerró dos tercios de esto**: `python_path` apuntaba a `Programacion\BAXY\src` —o sea que toda medición hecha aquí ejecutaba el `baxy_mind` del intento anterior— y ahora apunta a este repositorio; y `gguf` apunta a `D:\BAXYRuntimessets\models\Qwen3-4B-Q4_K_M.gguf`. Queda `llama_server`. *Goal 01 → parcialmente cerrado por el 03.*
- **Tesseract 5.4.0** — instalado y en PATH, pero sólo con `eng` y `osd`: **falta `spa.traineddata`**. El escalón OCR de la cascada leería español con el modelo inglés. Lo dispararía cualquier `ocr.read` sobre una ventana en español. *Goal 01 → lo necesita el 07.*
- **Wake word apagado** — `wake_manifest` es `null` y `wake_on_start` es `false` en el runtime, aunque `baxy.onnx` funciona (máx 0,916 en positivo, 0 disparos en negativo). Falta escribir su manifiesto. *Goal 01 → lo necesita el 09.*
- **STT y nombres propios** — Parakeet transcribe «BAXY» como «Maxi» / «Bacxi» / «Báxi» sobre voz humana real. El problema está documentado en `biasing_options.md` y sigue abierto; no bloquea porque el wake word es acústico y no depende de la transcripción. *Goal 01 → lo necesita el 09.*
- **`llama-server --reasoning-budget 0`** — en b9980 con la plantilla de Gemma 4 no quita el *thinking*: deja de separarlo y el borrador en inglés pasa a ser la respuesta visible. Lo dispararía usar esa bandera para recortar latencia. *Goal 01 → lo necesita el 06 y el 08.*
- **`Baxy.Setup`** — 13.194 líneas de producción y 10.068 de pruebas (17,8 % de `src/`) para una instalación certificada que está fuera de alcance. No se tira porque funciona, pero distorsiona cualquier cuenta de tamaño del repositorio. *Goal 01.*
- **13 GGUF de FunctionGemma** (5,8 GB) y **17 GB en `Probando Gemma 4/models/`** — iteraciones de fine-tuning que ya no se van a usar. No bloquea nada; es espacio en disco. *Goal 01.*
- **La poda de la importación dejó fuera 453 ficheros** que `Programacion\BAXY` sí versionaba. El goal 02 recuperó los 11 que las pruebas leían; los ~440 restantes (sobre todo `artifacts/development`, `product`, `research`, `fixes`, `holdout`) siguen ausentes. Lo dispararía cualquier prueba o documento futuro que cite uno. Se ve con `git ls-files` en los dos repositorios y `Compare-Object`. *Goal 02.*
- **Dos entradas de campaña perdidas para siempre** — `artifacts/development/bge_m3_operation_recovery_r160_attested.json` no está en ninguna carpeta de esta máquina, y el checkpoint `D:\BAXYRuntime\experiments\mtop-operation-compatibility-verifier-v16` de R208 tampoco (sólo queda `r242-cuda`). Sus preregistros publicados sí están y se auditan por sello; lo que no se puede es reconstruirlos. *Goal 02.*
- **`baxy_legacy_checkpoint` sigue colgando** — `artifacts/corpus_cutoff/source_manifest.json` declara también el commit `ee06786b…`, que no está en este repositorio. Hoy no lo pide nadie porque resuelve rutas bajo `/legacy/` y ese directorio no existe aquí. Lo dispararía traer `legacy/` de vuelta. *Goal 02.*
- **`experiments/mind_router_spike/.venv`** — el venv 3.10 que creó el goal 01 sigue en disco y ya no lo usa nadie: `FindPython` resuelve el intérprete del sistema. Es espacio, no un fallo. *Goal 02.*
- **`tests/data/historical_messages.jsonl` ya está versionado, y son datos privados.** El goal 02 lo comprometió porque decenas de pruebas .NET y Python lo leen por esa ruta, `.gitattributes` fija sus finales de línea, sus dos hermanos del mismo corpus ya estaban rastreados y sin él un clon limpio no puede pasar la compuerta. Pero `TURN_EVIDENCE_DATA_NOTICE.md` lo declara **dato privado local que no debe publicarse como dataset**, y el remoto `origin` es GitHub: **no debe empujarse a un remoto público**. Decidir si se queda así, se mueve a LFS o se sustituye por un derivado sin texto. *Goal 02.*
- **La puerta de dominio curada ya no falla cerrada por verbo extra en bluetooth/wifi** — `_curated_domain_is_grounded` para `bluetooth.radio.set` y `wifi.disconnect` exige el nombre del dominio, no una lista de verbos. «apagame el bluetooth» y «drop the wireless connection» dejan de morir por ausencia de lista. El resto de familias cubiertas sigue siendo whitelist positiva; 49 de 158 operaciones siguen sin regla. No se escribió un sexto gate. *Goal 04.*
- **El relleno de argumentos acierta 30,8 % y pregunta lo que ya está dicho** — 14 de 26 filas con el valor inequívoco en el texto contestan con una pregunta inútil: «abrime la carpeta de descargas» pregunta «¿abrir la carpeta de descargas?». No depende del decisor (Gemma 34,6 %, Qwen3 30,8 %) ni lo movió este goal. Lo dispararía cualquier petición con argumentos; hoy no bloquea porque la operación se elige antes. *Goal 03.*
- **Field UI en inglés de consola** (`ask, instruct, or paste`, `starting up…`, labels `sessions`/`memory`) — chrome de depuración, no la voz de BAXY. El censo no cuenta TSX. Lo dispararía usar Field UI como la ventana que lee una persona. *Goal 06.*
- **Progreso de turno sin prosa** — `FieldBridgeContract` publica etapa sin `label`. Si compose no llega a tiempo no se disfraza con «un momento…»; queda el spinner. Goal 08 mide la primera señal. *Goal 06.*
- **No hay GGUF Q3/Q2 de Qwen3-4B en disco** — no se descargó otro 2 GB para un A/B de cuantización del mismo modelo. Q4_K_M se conserva; reabrir cuando exista el candidato. *Goal 06.*
- **`NoteDisambiguation` todavía arma el menú de opciones en español** (`Encontré N notas llamadas…`) — es lista de elección, no confirmación de efecto; no coincidía con el censo de prosa. Sustituirlo pide los mismos hechos JSON. *Goal 06.*
- **Excepciones internas pasadas a inglés** (`The local request id is already in use.`) — no son voz; el censo las contaba por «petición». *Goal 06.*
- **Compose con hechos delgados** — **cerrado en el sidecar**: `compose_visible_defect` tira polaridad invertida, welcome con Listo, códigos y nombres no mencionados. Quedan frases cortas («Listo, Word.») que no se rechazan porque vaciaban el compose. *Goal 06.*
- **Confirmación que afirma** — **cerrado en compose**: tiene que ser pregunta con confirm* o cancel*; el afirmado con Listo se tira. *Goal 06.*
- **49 de 158 operaciones siguen sin ninguna regla de dominio curada** — la puerta devuelve `None` para ellas y sólo `filesystem.` tiene un suelo genérico. Lo dispararía una propuesta del modelo sobre una de esas familias ante una petición fuera de catálogo, como el `window.active` que «prende las luces del living» recuperó. *Goal 03.*
- **`_post_native_tool_selection` quedó sin usar por defecto** — el contrato de tool-call forzado se midió y se rechazó (+3 decisiones crudas, −8 abstenciones honestas), y ahora sólo lo enciende `BAXY_MIND_NATIVE_TOOL_POLICY=1`. Borrarlo entero toca `decide_turn` en varias ramas y merece su propio cambio; no bloquea porque ninguna ruta de producto lo alcanza. *Goal 03.*
- **El sello `wake-validation-program-tree` cubre todo `src/baxy_mind`** — cinco programas de `experiments/stt_quality` congelan el árbol de `experiments/voice_latency`, `scripts` y `src/baxy_mind` antes de abrir un holdout ciego de STT. Cualquier cambio de la política de turno, del planner o del cliente LLM lo pone rojo aunque el evaluador de STT no ejecute nada de eso. Repinado de `22f3bd4e…` a `2124b0a3…` por el goal 03 y de `2124b0a3…` a `b7f218e6…` por el 03B, las dos veces por el mismo motivo y sin que el evaluador de STT ejecute nada de lo que cambió. Lo volverá a disparar el siguiente goal que toque la mente. *Goal 03 → repetido en el 03B.*
- **Los primeros ~15 s tras arrancar, la recuperación es léxica** — `PlannerCatalog` se publica con ranking por solapamiento de tokens y se promociona a E5 cuando el worker del encoder termina de cargar. Es correcto para un producto que arranca con Windows y se queda, pero un turno en esa ventana recupera peor. Lo dispararía pedirle algo a BAXY nada más encender. *Goal 03.*
- **El corpus fresco del goal 03 lo escribió el modelo que hizo el goal** — 160 filas de autoría propia. Su independencia del reconocedor sí está medida (24 % de alcance), pero no es una población recogida de uso real. Lo dispararía tratar su 46,0 % como el número de producto en vez de como el número de este instrumento. *Goal 03.*
- **El reconocedor determinista sirve 26 de las 38 filas que reclama, y sus 12 errores son silenciosos** — resuelve una hermana y nadie se entera: «dejame el sonido a la mitad» acaba en `audio.status`. Se conserva porque el camino por modelo sirve 24 de esas mismas 38, o sea que gana por dos, y responde en 7 ms contra 2 s. **El goal 03B le dio la declinación**: el mismo verificador de identidad retira la resolución cuando no reconoce el efecto, y el reparto por causa baja de 12 a 7 filas. Lo dispararía cualquier paráfrasis que caiga dentro de su gramática por accidente. *Goal 03.*
- **Exentar las operaciones `read_only` de la puerta de dominio da +8 filas de 124 sin añadir un solo efecto no pedido, y está refutado** — «¿Cómo está la red neuronal?» propone `network.status`, que es read-only, y BAXY contestaría con la conectividad del equipo. La puerta protege también contra contestar del dominio equivocado, y `red`, `tiempo`, `memoria` y `página` son polisémicas así. Medido, revertido y publicado en `documentacion/base/03_COMPRENSION.md` §6 para que no se reintente sin ver el contraejemplo. *Goal 03.*
- **17 filas contestan «No puedo X» sobre algo que BAXY sí sabe hacer** — el veto de dominio marca `conversation_kind: "unsupported"`, que significa «no está en el catálogo», cuando lo que pasó es «no pude confirmar el dominio». La identidad dice que BAXY dice que no **sólo** a lo que no sabe hacer. **Cerrado por el goal 03B**: la etapa ya no borra la operación, la retiene y pide permiso; medido en `documentacion/base/03B_COMPRENSION_TECHO.md` §2. *Goal 03 → cerrado por el 03B.*
- **El sidecar dejó un `llama-server` huérfano al interrumpirlo** — 2,5 GB de VRAM retenidos durante horas tras una corrida cortada por timeout, compitiendo con las siguientes mediciones. `process_lifecycle.py` existe para reapear hijos y en las salidas normales lo hace; el camino que falla es la interrupción externa. Lo dispararía cualquier `Ctrl-C` o timeout sobre el arnés. *Goal 03.*
- **Las operaciones de argumento libre son sumideros de alcance** — `task.create`, `note.create`, `message.send` y `web.search` aceptan cualquier texto, así que `task.create("pedir un taxi")` es válido contra su schema. Once de cada veinte peticiones fuera de catálogo aterrizan ahí, y por eso ninguna abstención basada en contratos puede ser completa. Lo dispararía cualquier petición fuera de catálogo con forma de encargo. Medido en `documentacion/base/03_COMPRENSION.md` §6. *Goal 03.*
- **MTOP quedó descargado en `D:\BAXYRuntime\datasets\mtop-v1`** (8,6 MB de zip, 30 MB extraídos, SHA-256 verificado contra el declarado en el árbol) y su corpus de desarrollo construido. El test oficial sigue sellado y sin abrir. No bloquea nada; es espacio en disco y un activo para quien retome el alcance. *Goal 03.*
- **`.gitattributes` declara `* text=auto eol=lf` y Python en Windows escribe CRLF** — cualquier fichero regenerado con `write_text` sale con CRLF y su SHA-256 muere en el primer checkout limpio, que es el fallo que R277 diagnosticó. Y la mitad de `artifacts/` está comprometida con CRLF **en el índice** sin declaración `-text`, así que renormalizarla mueve hashes publicados. No hay nada en el árbol que avise: lo dispararía cualquier goal que regenere un fichero sellado. *Goal 03.*
- **`git config core.longpaths` no viaja con el repositorio** — sin él, `git worktree`/`clone` fallan con `Filename too long` sobre los artefactos de nombre largo, aunque Windows tenga `LongPathsEnabled=1`. Es provisión de máquina y está en `documentacion/base/00_COMPUERTA.md` §11; no hay forma de declararlo desde el árbol. *Goal 02.*
- **El decisor se va a conversar y ninguna etapa lo veta** — 12 filas de 124 terminan en una `conversation` que niega una capacidad del catálogo sin que ningún veto haya tocado nada: «dale enter» con `input.key.press` en el shortlist, «llevame a wikipedia» con `browser.navigate`, «type hello world for me» con `input.text.type`. Extender ahí la regla del 03B se midió y se rechazó (8 de 36 abstenciones honestas convertidas en preguntas, cero filas recuperadas). La línea viva es desacoplar la decisión de llamar de la del formato (arXiv 2606.25605), y el goal 03 ya rechazó la palanca opuesta. *Goal 03B.*
- **Las hermanas dentro de la familia son ahora el tramo dominante de la decisión** — 10 de las 18 filas que pierde la decisión eligen una hermana: `audio.volume.adjust` por `audio.volume`, `filesystem.known.search` por `filesystem.list`, `backup.create` por `backup.known.create`, `game.installed.named` por `app.installed`. Es la forma del catálogo, no el decisor. La forma paramétrica gana la recuperación (goal 03 §7) y su premisa aguas abajo se midió falsa con el shortlist de hoy; reabrirlo pide medir el relleno de argumentos primero. *Goal 03B.*
- **`unsupported_live_machine_query` es una sola expresión regular sobre la GPU** — `effect_intent.py`. Es lo único que impide, fuera del guarda de efecto retirado, que una respuesta de tipo `knowledge` invente el estado de esta máquina. El goal 03B cerró la cuarta forma que se había colado («what does this page actually say» devolvía el contenido de la página inventado) preguntándole al catálogo, pero la regla de vocabulario sigue ahí y sigue cubriendo un solo caso. *Goal 03B.*
- **El verificador de identidad añade un decodificado a cada retirada y a cada declinación** — p50 0,15 s por llamada, y el camino determinista pasa de p50 0,007 s a 0,16 s porque ahora consulta antes de publicar. Sigue diez veces por debajo del camino por modelo y el p90 de punta a punta no empeoró, pero es coste nuevo en la ruta más barata que tiene BAXY. Lo dispararía un decisor más lento o una máquina sin GPU. *Goal 03B.*
- **PRISMS lee activaciones internas y por eso no entra** — arXiv 2608.00218 detecta la sobrellamada de herramientas con una sonda logística sobre neuronas MLP, ROC-AUC 0,98–1,00. Adoptarlo obliga a leer el interior del decisor, lo que rompe la frontera de proceso que hace que cambiar de modelo no recompile nada, y ata la abstención a los pesos de un binario. Es el candidato con más fundamento para el goal que retome el alcance, y por eso queda anotado en vez de perseguido. *Goal 03B.*
- **Dos corridas del arnés del goal 03 a la vez se corrompen entre ellas** — `run_goal03_comprehension.py` deriva la ruta del audit del `--label`, pero el sidecar y el `llama-server` compiten por la GPU y una corrida lanzada en segundo plano que no murió sigue escribiendo. El síntoma es un `JSONDecodeError: Extra data` leyendo el audit, no un error de recursos, así que se diagnostica mal. Lo dispararía encadenar corridas en segundo plano. Se ve con `Get-Process llama-server`: si hay dos, hay dos corridas. *Goal 03B.*
- **El 90 % del goal 03B es imposible en este instrumento, y son trece filas concretas** — el reconocedor y la recuperación pierden las mismas 13 de 124 en las seis corridas medidas, así que el máximo con decisión perfecta y sin un solo veto es 111/124 = 89,5 %. Están nombradas una a una en `documentacion/base/03B_COMPRENSION_TECHO.md` §6. Lo que las movería no es una idea suelta: un decisor que distinga hojas casi sinónimas mejor que Qwen3-4B dentro de 4 GB, o un catálogo cuyas hojas no lo sean. *Goal 03B.*
- **La selección paramétrica en dos etapas está refutada de punta a punta** — elegir primero la familia y después la hoja acierta 13 de las 40 filas perdidas y **rompe 10 de 30 que ya se servían**, con 1,24 s de coste por turno. Es la confirmación end-to-end de lo que el goal 03 dedujo del ranking; no se reintenta sin un decisor distinto. *Goal 03B.*
- **Elegir la hoja dentro de la familia ya nombrada no paga** — mediana 82,5 de 124 contra 82 en siete corridas, y p50 de 2,30 s a 2,62 s. Elige mejor la hoja y la pierde después confirmándola: traslada filas del tramo de decisión al de veto. Implementada, medida y retirada. *Goal 03B.*
- **Subir el shortlist de 28 a 48 candidatos no paga** — la operación esperada se ofrece 3 veces más (109 → 112 de 124) y la decisión pierde 3 más (21 → 24), así que el neto es −2 filas, con p50 de 2,30 s a 2,61 s y la abstención honesta de 28 a 26 de 36. Es la misma curva que el goal 03 midió entre 8 y 28 candidatos, extendida: más candidatos compran recuperación y la pagan en decisión. *Goal 03B.*
- **El tope de candidatos está escrito dos veces y sólo una es la constante** — `planner.MAX_SHORTLIST_OPERATIONS` y un `28` literal en `_turn_candidates` de `llm.py`. Cambiar sólo la constante hace que cada turno muera con `ValueError` y que el arnés informe «el catálogo semántico no llegó a estar listo», que es un síntoma que apunta al sitio equivocado. Lo dispararía cualquier medición del tamaño del shortlist. *Goal 03B.*
- **Una política mínima que sólo devuelve `effect_operations` invoca con ansiedad** — sobre los candidatos congelados de `goal03_resume_baseline_20260820` mejora la decisión cruda de 83 a 89/124 y tarda p50 0,578 s, pero elige una operación en las 36 peticiones fuera de catálogo aunque el array vacío sea válido. Esa forma sin `no_operation` explícito queda rechazada. *Goal 03B, retoma 2026-08-20.*
- **El centinela `no_operation` no arregla la política mínima** — evita la mezcla formal y recupera 17/36 abstenciones honestas, pero baja la selección a 82/124 (p50 0,552 s), de vuelta al decisor vigente. Añadir una alternativa explícita cambia llamada por abstención; no mejora la frontera. *Goal 03B, retoma 2026-08-20.*
- **La selección escalar también queda refutada** — obligar al mismo Qwen3-4B a elegir una sola hoja o `no_operation` baja a 76/124, con 19/36 abstenciones honestas y p50 0,443 s; elimina listas espurias, pero empeora la selección. *Goal 03B, retoma 2026-08-20.*
- **Los mensajes históricos no son un set de entrenamiento representativo** — un clasificador de familias entrenado sin duplicados obtiene 93,28 % en su validación histórica y sólo 41/124 en el corpus fresco, aunque conserva 31/36 abstenciones; el cambio de distribución impide promoverlo. *Goal 03B, retoma 2026-08-20.*
- **Qwen3.5-4B mejora selección pero sobrellama sin las puertas de producto** — con el contrato mínimo sube de 82 a 91/124 y tarda p50 0,787 s, pero sólo abstiene 9/36; queda pendiente únicamente la medición end-to-end con los vetos vigentes. *Goal 03B, retoma 2026-08-20.*
- **Qwen3.5-4B queda refutado por el contrato completo** — las puertas recuperan 35/36 abstenciones, pero el producto cae a 58/124 (45 pérdidas de decisión) y p50/p90 4,445/5,702 s; no sustituye al Qwen3-4B registrado. *Goal 03B, retoma 2026-08-20.*
- **El ranker congelado no reemplaza E5, pero su unión mueve el techo** — solo termina en 109/124 a top-28 y 20/36 abstenciones top-1; unido simétricamente a E5 ofrece la hoja correcta en 119/124 con 28 candidatos. Falta un selector que cobre esa cobertura. *Goal 03B, retoma 2026-08-20.*
- **Un cabezal lineal sobre E5 tampoco selecciona la hoja** — entrenado con 4.740 paráfrasis actuales y sin solapamiento exacto, valida 98,50 % en su origen pero da 57/124 top-1 y 25/36 abstenciones frescas; sólo sirve como señal de recuperación (117/124 top-28). *Goal 03B, retoma 2026-08-20.*
- **Qwen3-8B IQ2 mueve la frontera, pero E5 todavía le pone techo** — con el contrato mínimo da 90/124, 29/36 abstenciones y p50 0,817 s; queda pendiente sólo medirlo sobre la unión ranker+E5 que ofrece 116–119 hojas. *Goal 03B, retoma 2026-08-20.*
- **La unión-16 convierte cobertura en siete aciertos, no en quince** — Qwen3-8B pasa a 97/124 con 28/36 abstenciones y p50 0,628 s sobre un techo de 116; ensanchar a 28 sólo añade tres hojas y más ambigüedad, así que el bloqueo restante es selección. *Goal 03B, retoma 2026-08-20.*
- **Deliberar dentro del schema compra cinco hojas y vende cinco abstenciones** — resumir verbo/objeto/postcondición antes de elegir sube a 102/124, baja a 23/36 abstenciones y deja 14 errores con la hoja ofrecida; no se promueve. *Goal 03B, retoma 2026-08-20.*
- **Pensamiento-64 mejora selección pero no cierra** — Qwen3-8B llega a 105/124 sobre unión-16, con 21/36 abstenciones y p50 1,469 s; justifica un solo escalón a 128 tokens, no una búsqueda de presupuestos. *Goal 03B, retoma 2026-08-20.*
- **Pensamiento-128 tiene rendimiento decreciente** — llega a 108/124 y 18/36 abstenciones con p50 2,297 s; la unión-oráculo con 64 tokens sólo da 109, así que votar presupuestos no cierra. Resta medir el mismo 128 sobre unión-28. *Goal 03B, retoma 2026-08-20.*
- **La política mínima queda agotada en 109/124** — pensamiento-128 sobre unión-28 añade sólo una fila, baja a 15/36 abstenciones y p50 2,428 s; la política registrada conserva tres aciertos distintos y la unión-oráculo da 112, por lo que resta medir el 8B en el pipeline completo. *Goal 03B, retoma 2026-08-20.*
- **Qwen3-8B cabe en VRAM pero el pipeline completo no cobra su selector** — punta a punta da 89/124, 23/36 abstenciones y p50 1,774 s, contra 109/124 del contrato mínimo; el incremento conservador observado es 3.417 MiB. Las pérdidas vuelven a concentrarse en decisión (18), así que cambiar sólo el modelo queda refutado. *Goal 03B, retoma 2026-08-20.*
- **El árbitro de propuestas queda a una fila y abre doce negativas** — la unión de seis selectores ofrece 118/124; un segundo pase 8B escalar elige 111/124, abstiene 24/36 y añade p50 2,075 s. Otra elección de prompt sobre el mismo corte sería ajuste al examen; retomarlo exige un árbitro o datos de selección independientes. *Goal 03B, retoma 2026-08-20.*
- **El FunctionGemma R2 heredado es reconstruible, pero no idéntico en pesos** — el corpus ganador se recuperó exactamente (1.821 filas, SHA `325a287f…`), y el control familia-oráculo da 64/74 frente a 65/74 histórico, p50 0,736 s y 602,4 MiB asignados. La única medida pendiente es usarlo sobre la unión ranker+E5 actual; no autoriza promoción por sí solo. *Goal 03B, retoma 2026-08-20.*
- **FunctionGemma R2 no generaliza del contraste intrafamilia a la unión real** — con 119/124 hojas recuperadas selecciona sólo 63/124 y abstiene 1/36; cabe de sobra (665,4 MiB, p50 0,717 s), pero fuera de su distribución casi siempre llama una operación. El árbitro por familia heredado ya empeoró esta forma en la campaña anterior. *Goal 03B, retoma 2026-08-20.*
- **`TIGHT_CLUSTER` no sobrevive al catálogo actual, pero la puerta adaptativa original sí** — cap 8 en desacuerdo baja la recuperación a 109/124; cap 4 sólo con top-1 concordante y cap 16 en el resto conserva 114/124 con 13,10 candidatos medios. Sólo esta última justifica inferencia. *Goal 03B, retoma 2026-08-20.*
- **La puerta adaptativa heredada pierde más selección de la que gana por limpieza** — Qwen3-8B baja de 109 a 99/124, aunque conserva 114 hojas, abstiene 21/36 y tarda p50 2,348 s; gana cuatro casos y pierde catorce frente a unión-28. Su oráculo conjunto da 113, pero elegir entre ambos sobre este mismo corte sería ajuste al examen. *Goal 03B, retoma 2026-08-20.*
- **El sentinel nativo desacopla formato, pero no desambigua hojas hermanas** — `tool_choice=required` + una tool heredada de no-match da 100/124, 24/36 abstenciones y p50 0,922 s sobre unión-28. Gana tres casos y pierde doce frente a pensamiento-128; doce negativas aún producirían acción, así que velocidad y forma correcta no bastan. *Goal 03B, retoma 2026-08-20.*
- **El sampling oficial de Qwen3 mueve una fila, no la frontera** — reemplazar greedy por temperatura 0,6/top-p 0,95/top-k 20/presence 1,5 en pensamiento-128 da 110/124 frente a 109, pero baja de 15 a 18 abstenciones y cambia 24 decisiones; no justifica barrer semillas ni escoger la favorable. *Goal 03B, retoma 2026-08-20.*
- **Qwen3-8B Q4 oficial cabe con offload, pero no mejora y rompe el silencio** — con 20/36 capas GPU da 109/124, 20/36 abstenciones y p50/p90 8,904/10,142 s; el incremento WDDM máximo fue 3.884 MiB. Gana cinco filas y pierde seis frente a IQ2, así que no se persiguen 64 tokens ni más capas. *Goal 03B, retoma 2026-08-20.*
- **La cabeza de abstención heredada no completa la guarda actual** — con su call-site desplegado abstiene 0/36 negativas; restaurar las features de intención evita cuatro acciones del selector 8B, pero baja 110→76/124 aciertos. El detector deíctico sólo reconoce `clp-02`, no `clp-03`/`inp-03`, y sin un resultado previo no tiene referente que inyectar. Medido con el encoder ONNX coordinado SHA `987091a8…`; no se integra. *Goal 03B, retoma 2026-08-20.*
- **El reranker mMARCO heredado no resuelve las hojas hermanas** — con suelo/top-k literales acierta 23/124 a top-1, 28/124 a top-3 y 0/14 errores residuales del selector; activarlo sólo en vacíos conserva 110/124 pero baja 18→8/36 abstenciones. Cuesta p50 106 ms CPU. El call-site existió en `3f2664c` y fue retirado en `406905a`; no había banda fusionada viva que copiar. *Goal 03B, retoma 2026-08-20.*
- **El command splitter heredado acierta sólo la frontera estructural de 7/15 misiones** — cuatro fronteras corresponden a fallos actuales (`cmp-b04/b05/b10/b15`) y justifican un probe por cláusula, pero 8/15 quedan subpartidas y contar cláusulas no acredita operaciones ni dependencias. Cuesta p50 3,015 ms; no se integra aún. *Goal 03B, retoma 2026-08-20.*
- **Procesar las cláusulas heredadas por separado no justifica otro orquestador** — en las cuatro misiones fallidas separables sube 0→1 misiones y 0→5/8 pasos nombrados, pero tres segundas cláusulas dependientes siguen fallando y `if it is, open it` no acredita la condición sin resultado previo. Cada decisión cuesta p50 2,555 s; el banco global ya cumple 5/15 y 13/32, así que la integración corresponde al goal 07. *Goal 03B, retoma 2026-08-20.*
- **El KVA coordinado preserva acciones pero no guarda este fuera-de-catálogo** — con encoder SHA `987091a8…` y umbral heredado 0,89 conserva 124/124 positivas y corta 0/36 negativas; sobre el selector oficial queda 110/124 y 18/36. Bajar el umbral usando el corpus sellado sería sobreajuste, así que no se integra. *Goal 03B, retoma 2026-08-20.*
- **El campeón FunctionGemma Tools-Reduce iter3 no transfiere al catálogo actual** — el GGUF promovido SHA `c6fe7e94…` da 19/124; fuera de catálogo emite 0 `no_tool`, seis acciones autorizables y 30 nombres inventados que el kernel rechazaría. Iter4 ya estaba rechazado por regresiones previas, por lo que no se barre. *Goal 03B, retoma 2026-08-20.*
- **Los bancos `dataset_finetune/out` no contienen el selector de hoja que falta** — publican recall de familias 0,9828/0,9790 cuando la unión actual ya recupera 119/124 hojas; sus 22 dev-fails no solapan exactamente el corte. El guard de replies que baja fabricaciones 7→0 ya está cubierto por veto de estado no leído + `OperationOutcome.Verified`, así que copiarlo duplicaría ownership. *Goal 03B, retoma 2026-08-20.*
- **La memoria de exemplars heredada no generaliza a las paráfrasis selladas** — con suelo 0,92 sólo matchea 1/160, no abstiene ninguna negativa y deja 110/124, 18/36 sin cambio; bajar el umbral sobre este corte sería calibración del examen. *Goal 03B, retoma 2026-08-20.*
- **El único challenger nuevo queda preregistrado, no promovido** — corpus FunctionGemma de unión real: train 4.222 acciones + 2.700 negativas, validación 477 + 307, 169 operaciones, 4.699/4.699 hojas recuperadas y cero solapamiento con el corte. Falta entrenar una sola LoRA con muestreo 2:1 y medir validación antes del examen. *Goal 03B, retoma 2026-08-20.*
- **La atención eager no permite entrenar el challenger largo en esta GPU** — el intento de una época acaba en OOM sin pesos tras más de 61,4 min; SDPA sí completa backward del peor caso de 1.392 tokens con 4.748,8 MiB asignados. Se autoriza sólo el reintento mecánico SDPA/1.408, sin cambiar datos ni hiperparámetros; los 4 GB se juzgarán en inferencia. *Goal 03B, retoma 2026-08-20.*
- **La LoRA de unión actual ya existe, pero aún no está promovida** — SDPA completa 2.028 filas/254 updates en 4.779,975 s, pérdida 0,004685→0,000030 y 4.786,6 MiB asignados; adapter SHA `61906906…`. Falta la validación independiente sellada y después, sólo si generaliza, inferencia fresca/VRAM. *Goal 03B, retoma 2026-08-20.*
- **La LoRA supera el holdout, pero aún no el examen del goal** — validación independiente: 447/477 acciones exactas, 296/307 `no_action` exactos y 307/307 sin llamada de acción; p50 0,789 s, 665,8 MiB. Autoriza una única corrida fresca sin calibración posterior. *Goal 03B, retoma 2026-08-20.*
- **El holdout FunctionGemma no representa las paráfrasis frescas** — el mismo adapter cae de 447/477 a 24/124; emite `no_action` en 89 positivas, aunque conserva 33/36 abstenciones y 665,4 MiB. Se rechaza sin recalibrar contra el examen; hace falta evidencia independiente de lenguaje real. *Goal 03B, retoma 2026-08-20.*
- **Los mensajes reales heredados existen, pero su vocabulario de acciones es estrecho** — 1.071 pares/1.018 textos únicos; FunctionGemma conserva 812 y puede mapear 1.365 filas históricas a sólo 42 funciones legacy. El train actual sólo heredó 68 textos. Antes de reentrenar hace falta medir crosswalk y cobertura del catálogo actual. *Goal 03B, retoma 2026-08-20.*
- **El catálogo legacy no puede servir de puente al catálogo actual** — 42 funciones reales cubren 44/169 operaciones y un oráculo generoso sólo 50/124 filas frescas. No se ejecuta ni integra; únicamente se conserva la posibilidad de reutilizar frases reales con traducción exacta como datos del selector actual. *Goal 03B, retoma 2026-08-20.*
- **El corpus real traducible es de estilo, no de cobertura** — el filtro exacto deja 345 textos sin solapamiento fresco: 274 train/17 operaciones y 71 validación/19. Antes de mezclarlo con las 169 hojas hay que medir recuperación ranker+E5 sin forzar. *Goal 03B, retoma 2026-08-20.*
- **La unión recupera la mayoría, no todos los labels reales heredados** — 242/274 train y 62/71 holdout sin forzar; quedan 242 filas/16 operaciones útiles y nueve misses honestos en validación. Antes de otra LoRA se medirá V28 tal cual sobre ese holdout. *Goal 03B, retoma 2026-08-20.*
- **V28 también falla en el holdout real independiente** — 11/71 exactos, con 49 órdenes enviadas a `no_action`; p50 0,756 s y 655,7 MiB. Se preregistra una sola LoRA mixta, que debe lograr ≥50/71 reales y preservar ≥430/477 sintéticos con 0/307 llamadas negativas antes del fresco. *Goal 03B, retoma 2026-08-20.*
- **La LoRA mixta queda sellada antes de entrenar** — 7.129 filas fuente, balance 2.028 con 96 mensajes reales/16 operaciones y 676 `no_action`, máximo 1.371 tokens; mismos hiperparámetros y gates ≥50/71 real, ≥430/477 sintético, 0/307 llamadas negativas. *Goal 03B, retoma 2026-08-20.*
- **La LoRA mixta terminó, pero todavía no está promovida** — completó las 2.028 filas/254 updates en 5.789,125 s, incluidos exactamente 96 mensajes reales; pérdida 0,000864→0,001086 y adapter SHA `f8b8f1fc…add04`. Los 4.726,6 MiB asignados son de entrenamiento, no el gate de inferencia; falta superar primero 50/71 en el holdout real sellado. *Goal 03B, retoma 2026-08-20.*
- **La LoRA mixta mejora el lenguaje real, pero falla su gate independiente** — sube 11→38/71, por debajo del 50/71 preregistrado; quedan 17 `no_action`, tres salidas no parseables y 13 hojas erróneas. Se rechaza sin ejecutar el holdout sintético ni volver al examen fresco; no se integra. *Goal 03B, retoma 2026-08-20.*
- **La única cabeza externa queda preregistrada, no promovida** — separa call/no-call con una logística `C=1` sobre E5 ya residente, entrenada con unión+274 acciones reales y sin barrido. Debe conservar 473/477 sintéticas y 70/71 reales, y rechazar 246/307 MASSIVE y 80/100 CLINC OOS antes de mirar el fresco o ejecutar Qwen. *Goal 03B, retoma 2026-08-20.*
- **El holdout real heredado tenía ocho cruces con el train de unión** — V28 queda 9/63 limpio y la LoRA mixta 30/63, todavía bajo el gate equivalente 45/63; otros tres cruces son con calibración. V42 abortó antes de cargar E5 o crear pesos, y V41 debe reseñarse sobre las 63 filas limpias. *Goal 03B, retoma 2026-08-20.*
- **V44 sustituye el preregistro contaminado sin tocar la receta** — la cabeza conserva E5/logística/datos y tres gates; sólo evalúa 60 acciones reales sin cruces y permite como máximo un veto (59/60). V45/v2 todavía no existen y se rechazarán antes del fresco si falla cualquier gate. *Goal 03B, retoma 2026-08-20.*
- **La cabeza call/no-call E5 repite el cambio de distribución** — pasa 473/477 acciones sintéticas, rechaza 302/307 MASSIVE y 99/100 CLINC (AUC 0,999269), pero conserva sólo 25/60 órdenes reales limpias. Se rechaza sin Qwen ni fresco; más negativos públicos o calibración de umbral repiten la familia MTOP ya fallida. *Goal 03B, retoma 2026-08-20.*
- **Los `logprobs` crudos de Qwen no son confianza de hoja bajo la gramática** — V46 confirma que el servidor heredado los expone, pero el grammar fuerza tokens muy improbables y la tokenización `action`/`none` es asimétrica; no se añadirá otra pasada de scoring. Sólo queda autorizada una política binaria explícita dentro de la misma inferencia, todavía sujeta a holdout independiente. *Goal 03B, retoma 2026-08-20.*
- **V47 sella la política When2Call dentro de Qwen, no la promueve** — decisión `action|none` antes de la hoja, una inferencia, contradicciones fail-closed; primero debe lograr ≥431/477 y ≤42/307 en sintético, después ≥55/60 real limpio, y sólo entonces puede ver el fresco. *Goal 03B, retoma 2026-08-20.*
- **La decisión When2Call dentro de Qwen conserva hojas pero no abstiene** — V48 logra 449/477 positivas, pero abre 187/307 acciones en negativas frente al máximo 42; 0/784 contradicciones de schema. Se rechaza antes de V49/fresco y no se barre prompt, enum ni semilla. *Goal 03B, retoma 2026-08-20.*
- **Qwen3.5-9B nativo queda como candidato, no como runtime** — el único UD-IQ2-XXS fijado carga en b9980 y pasa `app.open`/no-call en dos controles; su base publica BFCL-V4 66,1 frente a 50,3 del 4B. Faltan holdout independiente y medición real de VRAM; no se probarán otros quants. *Goal 03B, retoma 2026-08-20.*
- **V51 sella el holdout nativo de 9B, todavía sin autoridad** — contrato `tool_choice=auto` heredado, 99 capas y gates ≥431/477, ≤42/307, después ≥55/60 real limpio. Un fallo detiene V53/fresco; no se barrerán quant, prompt ni sampling. *Goal 03B, retoma 2026-08-20.*
- **La primera V52 no es una medición de calidad** — aborta en 401/784 por `arguments` nativos con JSON inválido; descarta las 400 filas en memoria y no crea V52/V53. Se reparará sólo el límite fail-closed, con salidas nuevas y sin cambiar modelo/prompt/gates. *Goal 03B, retoma 2026-08-20.*
- **V54 sella la reparación fail-closed antes de repetir el holdout** — respuestas nativas malformadas contarán como cero acciones e inconsistencia de schema; transporte/proceso/timeout siguen siendo fatales. V55 reinicia las 784 filas con los mismos gates y V56/fresco continúan prohibidos hasta que pase. *Goal 03B, retoma 2026-08-20.*
- **Qwen3.5-9B nativo guarda el alcance, pero pierde demasiadas hojas** — V55 deja sólo 21/307 acciones negativas, pero acierta 343/477 positivas frente al gate 431; una respuesta malformada queda fail-closed. Se rechaza antes de V56/fresco y no se barren quant, prompt ni sampling. *Goal 03B, retoma 2026-08-20.*
- **V57 agota las composiciones heredadas restantes sin relanzarlas** — mDeBERTa top-1 77,3 %, Ministral ya rechazado por latencia/VRAM, anti-primacía incapaz de arreglar alcance, y los guards FunctionGemma/SVM vetan 92/70 de 124 acciones legítimas. Se autoriza buscar una familia externa, no apilar estas piezas. *Goal 03B, retoma 2026-08-20.*
- **El tools API de llama-server no lee xLAM-2-3B** — el GGUF emite un JSON array en content y `tools` provoca HTTP 500 peg-native; el control v58 se abstuvo en `open calculator`. V58b inyecta la plantilla del GGUF como texto y pasa app.open + no-call. El holdout sintético es V59; fresco cerrado. Licencia de investigación, no se envía aunque gane. *Goal 03B, retoma 2026-08-21.*
- **xLAM-2-3B Q4_K_M no selecciona este catálogo** — V59: 180/477 hojas y 22/307 acciones negativas, p50 0,497 s. Abstiene bien y elige mal; 221 falsas abstenciones impiden usarlo de guarda. Se rechaza sin V60/fresco y sin otro quant. La siguiente familia no es un generador de function-calling. *Goal 03B, retoma 2026-08-21.*
- **Qwen3-Reranker-0.6B generaliza al sintético y no al habla real ni al fresco** — V61 433/477 y 3/307; V62 10/60; V63 congelado 63/124 y 0/36, top-1 crudo 90/124. El 0,5 oficial sobreveta el fresco; no se retoca. No sustituye al 8B ni entra al producto. *Goal 03B, retoma 2026-08-21.*
- **Los pesos del ranker v3 no estaban en este árbol** — el manifiesto y el vocabulario sí; el `.npz` de 8.766.990 bytes SHA `63de7aae…` se heredó de `Programacion\BAXY\artifacts\research\operation_shortlist_v3`. Sin él la unión 119/124 no era ejecutable. *Goal 03B, retoma 2026-08-21.*
- **La unión ranker+E5 no cobra en el 4B vigente** — e2e1 82/124 (111 ofrecidas, 28 de decisión); e2e2 81/124 (113 ofrecidas, veto 10). Se retira del planner. *Goal 03B, retoma 2026-08-21.*
- **«necesito el bloc de notas» no es un listado de notas** — `notas?` coincidía dentro de «bloc de notas». El reconocedor ahora distingue el inventario de notas del objeto Notepad. *Goal 03B, retoma 2026-08-21.*
- **Filtrar las hojas no grounded en vez de vetar la lista entera abre el fuera-de-catálogo** — offline sobre rec5e2e7 recuperaría net-01 y pkg-01, y dejaría keep en ooc-09/11/13/14/15/22/25/30. No se cambia la política all-or-nothing. *Goal 03B, retoma 2026-08-21.*
- **La pila FunctionGemma del router heredado ya estaba agotada** — abstain_head 110→76, mMARCO 0/14 residuales, deícticos sólo clp-02, splitter al goal 07, LoRA fresco 24/124. No se re-mide. *Goal 03B, retoma 2026-08-21.*
- **Filtrar hojas ungrounded sigue abriendo el fuera-de-catálogo** — reconfirmado en r2/r3/r4: cmp-01, net-01 y win-01 siguen siendo listas mixtas que el veto all-or-nothing tira enteras. No se cambia. *Goal 03C.*
- **`ooc-12` «commit and push my changes to git»** — en el 03C aterrizaba en `package.install.commit` (1 de 3). En el 04, una corrida lo publicó como `game.install.commit`. Ahora es contradicción near-miss (git commit ≠ install commit). *Goal 03C → cerrado por el 04.*
- **La papelera recuperable (`note.trash`, `task.delete`, `filesystem.known.trash.named`) no confirma en modo normal** — identidad pide confirmar borrar; `RecoverableDelete` sigue mapeado a `Reversible` porque confirmar esas hojas escondía `note_ambiguous` detrás de un reto y rompía el ciclo de notas. Queda confirmar `WorkLoss` (vaciar papelera, cerrar app, matar proceso, olvidar memoria). Lo dispararía tirar una nota o un fichero a la papelera recuperable sin que BAXY pregunte. *Goal 04.*
- **V9 no se abrió** — el único sello ciego sin consumir. Esta medición usa el corpus fresco del goal 03 (`761c1bc3…`), ya abierto. Abrir V9 sin scorer congelado lo quema. *Goal 04.*
- **El catálogo de aplicaciones de Windows trae nombres duplicados al plegar** — «Developer Command Prompt for VS 2022» aparece dos veces; `catalog.configure` ahora se salta el duplicado en vez de rechazar el handshake. No se desduplica en Core. *Goal 04.*
- **«¿Quieres que capture.screenshot?» (cap-01)** — nombra la hoja interna en la pregunta. No es un éxito no verificado ni un «un momento…». *Goal 04.*
- **`input.key.press` / `input.text.type`:** Win32 SendInput aceptado no es el efecto semántico en la app enfocada. Dispararlo aquí escribiría en esta sesión. *Goal 05.*
- **`window.move` / `resize` / `focus` / `minimize` / `maximize` / `restore`:** el observador HWND existe; no se mutó una ventana arbitraria del escritorio. Sólo se abrió y cerró el Bloc de notas que lanzó esta sesión. *Goal 05.*
- **SMTC / «está sonando»:** Windows expone `GlobalSystemMediaTransportControlsSessionManager`, pero sin sesión multimedia restaurable no se afirma reproducción. *Goal 05.*
- **`system.settings.status` brillo:** WMI devolvió `brightness_status_verification_failed` en dos lecturas. No se toca el brillo del usuario. *Goal 05.*
- **`audio.volume.adjust` y `audio.microphone.mute` siguen por el handler externo** — el volumen absoluto y el mute de salida ya postleen el endpoint en Core; no se unificó el catálogo (eso es forma, goal 03). *Goal 05.*
- **La matriz MVP externa (`actualEffectsExecuted=0`) sigue como prueba de contrato** — recibo no verificado → rechazo. No es pass de ejecución. No se borra porque todavía cubre el borde del provider inerte. *Goal 05.*
