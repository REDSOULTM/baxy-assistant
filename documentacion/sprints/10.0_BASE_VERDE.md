# Goal 10.0 — Restaurar la base verde

> Grok 4.6 High, `/goal`, sesión nueva. Aplica íntegro
> `10_PROTOCOLO_GROK46.md`. Punto de partida: `2377b69`. Siguiente: 10.1.

## Objetivo único

Hacer que la compuerta Full sea coherente con el contrato estructurado de prosa que
ya existe al cierre del Goal 09, sin cambiar conducta de producto ni reintroducir
respuestas visibles fijas.

## Evidencia inicial

`scripts/test_source_quality.ps1 -Mode Full` pasa estática, build, Contracts 60/60,
Kernel 137/137, Providers 451/451 y Setup 477/477. Integration termina **2.742
pass, 85 fail y 1 skip**. Los fallos muestreados comparan frases naturales exactas
con JSON de `OperationOutcomeNarration` (`kind`, `operation`, `polarity`,
`verified`, `observed`, `error`): las pruebas conservan el contrato anterior a Goal
6, mientras el producto ya entrega hechos estructurados para composición por modelo.

## Recinto

Clasifica los 85 antes de editar. Si comparten esa causa, actualiza helpers y tests
para afirmar semántica/esquema/hechos, nunca una frase fija. Si alguno descubre un
defecto real distinto, corrige sólo su owner mínimo y deja test de regresión. No
toques corpus ni comportamiento de Goal 10.

Investiga el único skip `OptInRealRuntimeTraversesShellMindAndCoreUsingReadOnlyOperations`.
Si es aceptación in-scope, prepara el preflight o termina `FALLO_DE_AMBIENTE` con
receta para el dueño; no lo cuentes como pass. Si es deliberadamente opt-in y no
forma parte de Full, demuestra esa frontera en el gate en vez de asumirla.

## Criterios de cierre

- [ ] Los 85 fallos están inventariados por causa; ninguna expectativa fija se
      conserva o se traslada a otro helper.
- [ ] Integration verde con assertions sobre contrato estructurado y conducta, sin
      bajar cobertura ni convertir fallos en skips.
- [ ] El skip queda ejecutado o clasificado con evidencia; jamás contado como pass.
- [ ] `scripts/test_source_quality.ps1 -Mode Full` verde hasta el final, incluidas
      las suites Python que el rojo previo no alcanzó.
- [ ] Árbol fuera de pruebas cambia sólo si un defecto real lo exige; diff revisado,
      handoff, commit y push. Siguiente `10.1_CORPUS_Y_COLA.md`.
