# Goal 10.0 — Restaurar la base verde

> Grok 4.6 High, `/goal`, sesión nueva. Aplica íntegro
> `10_PROTOCOLO_GROK46.md`. Goal 09.5.12 cerró el linaje; este prompt queda
> habilitado como siguiente lanzamiento. Full verde del árbol 09.5 es la evidencia
> de partida. La referencia histórica `2377b69` explica el reinicio, no sustituye
> el baseline heredado final. Siguiente: 10.1.

## Herencia 09.5

Cita: `09.5.11C Full verde; 09.5.9:llm_decisor, runtime_inferencia, journal`.
Campaña `transplant` vacía (`conservar_actual`, cero lotes): no retires el mecanismo vivo ni construyas una segunda vía.
Hueco probado: ninguno en este owner; hereda la pieza citada y no la reconstruye.
Rechazos protegidos que no reentran: `functiongemma-270m-ft`, `qwen-vl`, `ollama-runtime`, `auto_approve`, `soak-24h-as-requirement`, `gemma-native-audio`.

## Contrato de sesión

Dependencia: `09.5.12_INTEGRAR_Y_REPLANIFICAR.md`. Siguiente: `10.1_CORPUS_Y_COLA.md`.
Presupuesto: ventana de trabajo <500k tokens; compacta y continúa en esta meta.
Ambiente: `FALLO_DE_AMBIENTE` pausa esta meta; no skip ni pass ambiental.
Barras que no se rebajan: 200 turnos, 1.947/808, 2.036 contratos, Identidad, Full.
Idioma: sólo ES/EN/spanglish.

## Objetivo único

Confirmar —y sólo si todavía hace falta, reparar— que la compuerta Full es coherente
con el contrato estructurado de prosa heredado, sin cambiar conducta de producto ni
reintroducir respuestas visibles fijas.

## Evidencia inicial

Antes de 09.5, `scripts/test_source_quality.ps1 -Mode Full` pasaba estática, build, Contracts 60/60,
Kernel 137/137, Providers 451/451 y Setup 477/477. Integration termina **2.742
pass, 85 fail y 1 skip**. Los fallos muestreados comparan frases naturales exactas
con JSON de `OperationOutcomeNarration` (`kind`, `operation`, `polarity`,
`verified`, `observed`, `error`): las pruebas conservan el contrato anterior a Goal
6, mientras el producto ya entrega hechos estructurados para composición por modelo.

09.5.11C debe haber ejecutado Full otra vez. Si ya está verde, verifica la evidencia
y cierra este preflight sin editar. Si reaparece el defecto, aplica el recinto.

## Recinto

Clasifica los 85 antes de editar. Si comparten esa causa, actualiza helpers y tests
para afirmar semántica/esquema/hechos, nunca una frase fija. Si alguno descubre un
defecto real distinto, corrige sólo su owner mínimo y deja test de regresión. No
toques corpus ni comportamiento de Goal 10.

Investiga el único skip `OptInRealRuntimeTraversesShellMindAndCoreUsingReadOnlyOperations`.
Si es aceptación in-scope, prepara el preflight o pausa la meta en
`FALLO_DE_AMBIENTE` con receta para el dueño; no lo cuentes como pass. Si es
deliberadamente opt-in y no
forma parte de Full, demuestra esa frontera en el gate en vez de asumirla.

## Criterios de cierre

- [ ] Los 85 fallos están inventariados por causa; ninguna expectativa fija se
      conserva o se traslada a otro helper.
- [ ] Integration verde con assertions sobre contrato estructurado y conducta, sin
      bajar cobertura ni convertir fallos en skips.
- [ ] El skip queda ejecutado o clasificado con evidencia; jamás contado como pass.
- [ ] `scripts/test_source_quality.ps1 -Mode Full` verde hasta el final, incluidas
      las suites Python que el rojo previo no alcanzó.
- [ ] Árbol fuera de pruebas cambia sólo si un defecto real lo exige; diff revisado,
      handoff, commit y push. Siguiente `10.1_CORPUS_Y_COLA.md`.
